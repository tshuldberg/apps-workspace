# MyNews environment matrix

GENERATED FILE. Do not hand-edit. Produced by `node scripts/gen-mynews-env-matrix.mjs` and verified in CI by `pnpm check:mynews-env-matrix`.

Every variable name and every file reference below is extracted from source, so this table cannot drift from the code. Regenerate it whenever a MyNews source file adds or removes an environment read, or when a read moves to a different file. Line numbers are deliberately not recorded: they go stale on edits that change nothing about the environment contract. The verifier fails on a stale file and on any variable that has no annotation.

**How the three non-derivable columns work.** Source cannot say whether a variable is required, whether it is a secret, or what it is for. Those three columns come from a hand-maintained annotation map inside the generator. A variable the scan finds with no annotation renders as `UNANNOTATED` and makes the verifier exit non-zero, so a new operational variable cannot be added silently.

**Scan roots.**

- `supabase/functions/mynews-*/**/*.ts` and `supabase/functions/_shared/mynews-*.ts`, matching `env('NAME')`, `Deno.env.get('NAME')`, and reads through a resolved name constant such as `env(ENV_SCREENING_VENDOR_URL)`.
- `apps/mynews-web/**/*.{ts,tsx}` and `apps/mynews-console/**/*.{ts,tsx}`, matching `process.env.NAME`, `process.env['NAME']`, `process.env[CONST]`, and indirect reads through an `env` map parameter such as `env.MYNEWS_SUPABASE_URL`. Console constants come from `apps/mynews-console/lib/env-names.ts`.
- `apps/mynews/**/*.{ts,tsx,mjs}` plus `app.json` and `app.config.*`, matching the patterns above and bare `EXPO_PUBLIC_MYNEWS_*` / `EXPO_PUBLIC_REVENUECAT_*` identifiers.

**Exclusions.** `node_modules`, `dist`, `build`, `coverage`, `.next`, `.expo`, `.turbo`, `__tests__`, `__fixtures__`, `__snapshots__`, `playwright-report`, `test-results`, any `test/`, `tests/`, or `e2e/` directory, any `*.test.*` or `*.spec.*` file, and any `playwright.config.*` / `vitest.config.*` / `jest.config.*` file. A test-only environment read is not an operational requirement, so it is deliberately absent from this document, and so are the test-harness variables that only the Playwright and Vitest runners read. This is a known blind spot: if a variable is genuinely needed at runtime, it must be read from non-test source or it will not appear here.

## Supabase Edge Functions (Deno)

Server-side secrets. Set with `supabase secrets set` against the MyNews project, never committed. Reads inside `_shared/mynews-*.ts` apply to every `mynews-*` function that imports that module.

| Variable | Required | Secret or public | Consumed by (file) | Purpose |
| --- | --- | --- | --- | --- |
| `MYNEWS_ACCOUNT_WORKER_SECRET` | required | secret | `supabase/functions/mynews-account-worker/index.ts` | Shared secret mynews-account-worker compares against the X-MyNews-Worker-Secret header. verify_jwt is off for this function, so an unset secret means the worker refuses every call (401) rather than running unauthenticated. The same value goes into nw_job_config key account_worker_secret for the pg_cron caller. |
| `MYNEWS_DMCA_RATE_SALT` | required | secret | `supabase/functions/mynews-dmca/index.ts` | HMAC salt (at least 32 characters) used to hash the DMCA submitter email and signed platform IP into the durable nw_consume_dmca_rate_limit token-bucket keys, so the rate limiter never stores a raw identifier. The edge function and the web BFF route must hold the SAME value or the buckets split. Rotating it resets every in-flight bucket. |
| `MYNEWS_HEALTH_SECRET` | required for the detailed health payload | secret | `supabase/functions/mynews-health/index.ts` | Gates the DETAILED mynews-health payload (queue depths, ages, worker heartbeats) behind the X-MyNews-Worker-Secret header. The shallow {status, checkedAt} payload is public by design so an uptime probe and scripts/mynews-smoke.sh work without a credential. Unset makes the detailed payload UNAVAILABLE (503), never open: an unset secret must not read as "no authentication required". |
| `MYNEWS_NCII_HASH_VENDOR_KEY` | optional, required when the hash vendor URL is set | secret | `supabase/functions/mynews-ncii-worker/seams.ts` | API key for the NCII hash-matching vendor. Missing key leaves the seam unconfigured. |
| `MYNEWS_NCII_HASH_VENDOR_URL` | optional | public | `supabase/functions/mynews-ncii-worker/seams.ts` | Hash-matching vendor endpoint (StopNCII or PhotoDNA class) in mynews-ncii-worker/seams.ts. Unconfigured routes the case to human review and never fabricates a match. |
| `MYNEWS_NCII_WORKER_SECRET` | required | secret | `supabase/functions/mynews-ncii-worker/index.ts` | Shared secret mynews-ncii-worker compares against X-MyNews-Worker-Secret. Unset means the 24h/48h urgent-case backstop never runs, so treat it as launch-blocking. Mirrored into nw_job_config key ncii_worker_secret. |
| `MYNEWS_NCMEC_CYBERTIPLINE_CREDENTIALS` | optional, founder-ops | secret | `supabase/functions/mynews-ncii-worker/seams.ts` | NCMEC CyberTipline credentials. Unset today. Submission stays founder-ops until the relationship exists. |
| `MYNEWS_NCMEC_CYBERTIPLINE_URL` | optional, founder-ops | public | `supabase/functions/mynews-ncii-worker/seams.ts` | NCMEC CyberTipline submission endpoint. The vendor relationship is not onboarded, so this is unset today and the worker records a pending human-review state instead of inventing a report reference. |
| `MYNEWS_PAYMENTS_CANCEL_URL` | required when payments are enabled | public | `supabase/functions/mynews-support/index.ts` | Redirect target Stripe Checkout returns a reader to after an abandoned support charge. |
| `MYNEWS_PAYMENTS_PROVIDER` | optional | public | `supabase/functions/mynews-support/index.ts` | Selects the support rail provider. Only the exact value `stripe` turns the live rail on; anything else, including unset, leaves the rail off and the capability detector reports payments as unavailable. |
| `MYNEWS_PAYMENTS_SUCCESS_URL` | required when payments are enabled | public | `supabase/functions/mynews-support/index.ts` | Redirect target Stripe Checkout returns a reader to after a successful support charge. |
| `MYNEWS_PAYMENTS_WEBHOOK_SECRET` | required when payments are enabled | secret | `supabase/functions/mynews-payments-webhook/index.ts` | Stripe webhook signing secret. mynews-payments-webhook verifies every event signature against it and rejects the request when it is unset, so an unset secret means charges and payouts never reach the ledger. |
| `MYNEWS_PAYOUT_REFRESH_URL` | required when payments are enabled | public | `supabase/functions/mynews-support/index.ts` | Stripe Connect onboarding refresh URL for a journalist whose onboarding link expired. |
| `MYNEWS_PAYOUT_RETURN_URL` | required when payments are enabled | public | `supabase/functions/mynews-support/index.ts` | Stripe Connect onboarding return URL for a journalist who finished the payout setup flow. |
| `MYNEWS_PROCESSOR_CLEANUP_SECRET` | optional, required when the cleanup URL is set | secret | `supabase/functions/mynews-account-worker/seams.ts` | Bearer secret for the processor-cleanup POST. Without it the seam stays skipped-unconfigured instead of calling an unauthenticated endpoint. |
| `MYNEWS_PROCESSOR_CLEANUP_URL` | optional | public | `supabase/functions/mynews-account-worker/seams.ts` | Processor-cleanup seam in mynews-account-worker/seams.ts. Unconfigured records the visible status skipped-unconfigured; it is never rewritten as done. |
| `MYNEWS_SCREENING_VENDOR_KEY` | optional, required when the screening vendor URL is set | secret | `supabase/functions/_shared/mynews-screening-gate.ts` | API key for the screening vendor. Missing key leaves the vendor pass unconfigured. |
| `MYNEWS_SCREENING_VENDOR_NAME` | optional | public | `supabase/functions/_shared/mynews-screening-gate.ts` | Vendor label recorded on screening decisions. Defaults to the literal string vendor. |
| `MYNEWS_SCREENING_VENDOR_URL` | optional | public | `supabase/functions/_shared/mynews-screening-gate.ts` | Optional escalate-only screening vendor called by _shared/mynews-screening-gate.ts. A vendor may only raise a class score, never clear a humanOnly class, and an absent vendor is the explicit unconfigured state rather than a verdict. |
| `MYNEWS_STRIPE_SECRET_KEY` | required when payments are enabled | secret | `supabase/functions/mynews-support/index.ts` | Stripe secret key mynews-support uses to open Checkout sessions and Connect onboarding links. |
| `MYNEWS_SUPPORT_WORKER_SECRET` | required | secret | `supabase/functions/mynews-support-worker/index.ts` | Shared secret mynews-support-worker compares against X-MyNews-Worker-Secret before running support-ledger reconciliation. Mirrored into nw_job_config for the cron caller. |
| `SUPABASE_SERVICE_ROLE_KEY` | required | secret | `supabase/functions/_shared/mynews-observability.ts`<br>`supabase/functions/_shared/mynews-payments-store.ts`<br>`supabase/functions/_shared/mynews-store.ts`<br>`supabase/functions/mynews-health/index.ts` | Service-role credential for every mynews edge write and RPC call. Bypasses RLS, so it never leaves the function runtime. (injected by the Supabase edge runtime) |
| `SUPABASE_URL` | required | public | `supabase/functions/_shared/mynews-observability.ts`<br>`supabase/functions/_shared/mynews-payments-store.ts`<br>`supabase/functions/_shared/mynews-store.ts`<br>`supabase/functions/mynews-health/index.ts` | Project REST base the mynews store, payments store, and worker-run recorder concatenate onto /rest/v1. (injected by the Supabase edge runtime) |

## Public website (apps/mynews-web, Next.js)

Server-side env for the public site. Nothing here is `NEXT_PUBLIC_`, so no value is inlined into the client bundle; the site reads them per request in Server Components, route handlers, and middleware.

| Variable | Required | Secret or public | Consumed by (file) | Purpose |
| --- | --- | --- | --- | --- |
| `MYNEWS_DMCA_EMAIL` | required for launch | public | `apps/mynews-web/lib/capabilities.ts` | Copyright and DMCA address published on /legal/dmca. Same placeholder rule as the legal address. |
| `MYNEWS_DMCA_RATE_SALT` | required | secret | `apps/mynews-web/app/api/dmca/route.ts` | HMAC salt (at least 32 characters) used to hash the DMCA submitter email and signed platform IP into the durable nw_consume_dmca_rate_limit token-bucket keys, so the rate limiter never stores a raw identifier. The edge function and the web BFF route must hold the SAME value or the buckets split. Rotating it resets every in-flight bucket. |
| `MYNEWS_FUNCTIONS_URL` | optional | public | `apps/mynews-web/lib/capabilities.ts` | Explicit functions origin override for the public site. Unset is normal: the cloud adapter and the capability detector both default to ${MYNEWS_SUPABASE_URL}/functions/v1, so an unset value is not treated as "no functions". Set it only when the functions host differs from the project host. scripts/mynews-smoke.sh reads the same name as its probe target. |
| `MYNEWS_LEGAL_EMAIL` | required for launch | public | `apps/mynews-web/lib/capabilities.ts` | DSA contact address. Unset leaves the module placeholder, which the capability detector classifies as uncontrolled and which makes the legal bundle omit the contact line rather than publish a dead address. |
| `MYNEWS_PAYMENTS_ENABLED` | optional | public | `apps/mynews-web/lib/capabilities.ts` | Affirmative support-rail flag for the public site. Missing, malformed, or placeholder values keep payments off, so the legal bundle never publishes the 2 percent platform-fee claim for a rail this deployment does not have. |
| `MYNEWS_PAYMENTS_PROVIDER` | optional | public | `apps/mynews-web/lib/capabilities.ts` | Selects the support rail provider. Only the exact value `stripe` turns the live rail on; anything else, including unset, leaves the rail off and the capability detector reports payments as unavailable. |
| `MYNEWS_PUBLIC_ORIGIN` | required for launch | public | `apps/mynews-web/lib/origin.ts` | Canonical origin for absolute URLs in RSS, sitemap, and OpenGraph tags. Unset falls back to the placeholder in lib/origin.ts, which would publish wrong canonical URLs, so set it before any public launch. |
| `MYNEWS_SAFETY_EMAIL` | required for launch | public | `apps/mynews-web/lib/capabilities.ts` | Safety and NCII reporting address published in the legal bundle. Same placeholder rule as the legal address. |
| `MYNEWS_SUPABASE_ANON_KEY` | required | public | `apps/mynews-web/lib/capabilities.ts`<br>`apps/mynews-web/lib/env.ts`<br>`apps/mynews-web/middleware.ts` | Publishable anon key the site sends on its REST and functions calls. |
| `MYNEWS_SUPABASE_URL` | required | public | `apps/mynews-web/lib/capabilities.ts`<br>`apps/mynews-web/lib/env.ts`<br>`apps/mynews-web/middleware.ts` | Supabase project base the public site reads articles through. Unset renders the honest not-connected state instead of an empty feed. |
| `MYNEWS_TRUSTED_CLIENT_IP_HEADER` | required for anonymous DMCA intake | public | `apps/mynews-web/app/api/dmca/route.ts` | Names the ONE inbound header the deployment guarantees its trusted edge sets and overwrites (Vercel: x-vercel-forwarded-for; Cloudflare: cf-connecting-ip; Fly: fly-client-ip). The mynews-web DMCA BFF reads only this header for the anonymous rate-limit IP and signs it for the edge. Unset means no trusted client IP can be derived, so anonymous DMCA submissions fail closed (503). It must name a header the proxy overwrites, or an attacker can spoof it; the app cannot verify that, so this is an operator responsibility. |
| `MYNEWS_WEB_REPORTING_ENABLED` | optional | public | `apps/mynews-web/lib/capabilities.ts` | Affirmative flag for the web reporting surface. Anything other than an explicit truthy value keeps web reporting reported as unavailable. |
| `NODE_ENV` | required | public | `apps/mynews-web/middleware.ts`<br>`apps/mynews-web/next.config.ts` | Set by the Next.js build and runtime, not by an operator. apps/mynews-web/middleware.ts relaxes one security header outside production based on it. (set by the Next.js build and runtime) |

## Moderation console (apps/mynews-console, Next.js)

The console reads its env indirectly through the name constants in `lib/env-names.ts` (`process.env[ENV_SUPABASE_URL]`), so this scan resolves those constant values.

| Variable | Required | Secret or public | Consumed by (file) | Purpose |
| --- | --- | --- | --- | --- |
| `MYNEWS_CONSOLE_MODERATOR_EMAILS` | required | public | `apps/mynews-console/lib/env.ts`<br>`apps/mynews-console/middleware.ts` | Comma-separated moderator allowlist. An empty or unset list authorizes NOBODY: middleware and requireModerator() both fail closed, so the console is unusable until it is set. That is the intended default. |
| `MYNEWS_CONSOLE_SUPABASE_ANON_KEY` | required | public | `apps/mynews-console/lib/env.ts`<br>`apps/mynews-console/middleware.ts` | Publishable anon key for the console session client (magic-link sign-in and cookie refresh). |
| `MYNEWS_CONSOLE_SUPABASE_SERVICE_ROLE_KEY` | required | secret | `apps/mynews-console/lib/env.ts` | Service-role key the console server actions use to read service-role-only tables (nw_ncii_cases, nw_dmca_notices, nw_moderation queue) and to call the enforcement RPCs. Server-only: lib/env.ts is marked server-only so it can never be imported into a client component. |
| `MYNEWS_CONSOLE_SUPABASE_URL` | required | public | `apps/mynews-console/lib/env.ts`<br>`apps/mynews-console/middleware.ts` | Supabase project base for the moderation console. Unset makes middleware redirect every request to /login and the login page show the not-configured banner. |
| `NODE_ENV` | required | public | `apps/mynews-console/middleware.ts` | Set by the Next.js build and runtime, not by an operator. apps/mynews-web/middleware.ts relaxes one security header outside production based on it. (set by the Next.js build and runtime) |

## Mobile app (apps/mynews, Expo)

Every `EXPO_PUBLIC_*` value is embedded in the shipped binary and is therefore PUBLIC by construction. Never put a service-role key, worker secret, or HMAC salt behind an `EXPO_PUBLIC_` name.

| Variable | Required | Secret or public | Consumed by (file) | Purpose |
| --- | --- | --- | --- | --- |
| `EAS_BUILD_PLATFORM` | required in EAS Build | public | `apps/mynews/scripts/check-build-env.mjs` | Injected by EAS Build. Tells check-build-env.mjs which RevenueCat key name (ios or android) must be present and prefix-valid for this build. (injected by EAS Build) |
| `EAS_BUILD_PROFILE` | required in EAS Build | public | `apps/mynews/scripts/check-build-env.mjs` | Injected by EAS Build. apps/mynews/scripts/check-build-env.mjs enforces the full release env contract only when this is the production profile, and reports skipped otherwise. A local run with it unset therefore checks nothing, which is why the gate belongs in the EAS build step. (injected by EAS Build) |
| `EXPO_OS` | required | public | `apps/mynews/app/(root)/data/runtime-capabilities.ts` | Injected by Expo Router at runtime. runtime-capabilities.ts reads it to pick the ios or android RevenueCat key. Not operator-set. (injected by the Expo runtime) |
| `EXPO_PUBLIC_MYNEWS_DMCA_EMAIL` | required | public | `apps/mynews/app/(root)/data/runtime-capabilities.ts`<br>`apps/mynews/app/(root)/legal.tsx`<br>`apps/mynews/scripts/check-build-env.mjs` | Copyright and DMCA contact shown on the in-app legal screen and pinned against release-manifest.json. |
| `EXPO_PUBLIC_MYNEWS_ENTITLEMENTS_TEST_MODE` | must be unset for release | public | `apps/mynews/scripts/check-build-env.mjs` | Development entitlement bypass. check-build-env.mjs fails a release build when it is the string true, so a build can never ship with paid features unlocked for everyone. |
| `EXPO_PUBLIC_MYNEWS_FUNCTIONS_URL` | required | public | `apps/mynews/app/(root)/data/launch-environment.ts`<br>`apps/mynews/app/(root)/data/runtime-capabilities.ts`<br>`apps/mynews/scripts/check-build-env.mjs` | Edge functions origin. Optional at runtime because the adapter defaults to ${baseUrl}/functions/v1, but check-build-env.mjs requires an explicit production value for a release build. |
| `EXPO_PUBLIC_MYNEWS_LEGAL_EMAIL` | required | public | `apps/mynews/app/(root)/data/runtime-capabilities.ts`<br>`apps/mynews/app/(root)/legal.tsx`<br>`apps/mynews/scripts/check-build-env.mjs` | DSA contact shown on the in-app legal screen and pinned against release-manifest.json by check-build-env.mjs. |
| `EXPO_PUBLIC_MYNEWS_PAYMENTS_ENABLED` | optional | public | `apps/mynews/app/(root)/data/runtime-capabilities.ts`<br>`apps/mynews/scripts/check-build-env.mjs` | Affirmative support-rail flag. check-build-env.mjs requires it to agree with release-manifest.json in both directions, so a manifest that claims the rail is live fails the build when this is off, and the reverse also fails. |
| `EXPO_PUBLIC_MYNEWS_PAYMENTS_PROVIDER` | optional, required when payments are enabled | public | `apps/mynews/app/(root)/data/runtime-capabilities.ts`<br>`apps/mynews/scripts/check-build-env.mjs` | Support-rail provider name for the app capability detector. Must agree with release-manifest.json. |
| `EXPO_PUBLIC_MYNEWS_REVENUECAT_API_KEY` | optional fallback | public | `apps/mynews/app/(root)/data/launch-environment.ts`<br>`apps/mynews/app/(root)/data/runtime-capabilities.ts` | Single-key fallback used when the platform-specific key is absent. Still prefix-validated per platform, so one key cannot serve both stores. |
| `EXPO_PUBLIC_MYNEWS_REVENUECAT_API_KEY_ANDROID` | required for Android release | public | `apps/mynews/app/(root)/data/launch-environment.ts`<br>`apps/mynews/app/(root)/data/runtime-capabilities.ts`<br>`apps/mynews/scripts/check-build-env.mjs` | Public RevenueCat SDK key for the Play build. Prefix-validated as goog_. |
| `EXPO_PUBLIC_MYNEWS_REVENUECAT_API_KEY_IOS` | required for iOS release | public | `apps/mynews/app/(root)/data/launch-environment.ts`<br>`apps/mynews/app/(root)/data/runtime-capabilities.ts`<br>`apps/mynews/scripts/check-build-env.mjs` | Public RevenueCat SDK key for the App Store build. Prefix-validated as appl_; a mismatched prefix disables subscriptions with honest copy instead of failing at purchase time. |
| `EXPO_PUBLIC_MYNEWS_SAFETY_EMAIL` | required | public | `apps/mynews/app/(root)/data/runtime-capabilities.ts`<br>`apps/mynews/app/(root)/legal.tsx`<br>`apps/mynews/scripts/check-build-env.mjs` | Safety and NCII contact shown on the in-app legal screen and pinned against release-manifest.json. |
| `EXPO_PUBLIC_MYNEWS_SUPABASE_ANON_KEY` | required | public | `apps/mynews/app/(root)/data/launch-environment.ts`<br>`apps/mynews/app/(root)/data/runtime-capabilities.ts`<br>`apps/mynews/scripts/check-build-env.mjs` | Publishable anon key the fetch-based cloud adapter sends. Release builds are rejected without it. |
| `EXPO_PUBLIC_MYNEWS_SUPABASE_URL` | required | public | `apps/mynews/app/(root)/data/launch-environment.ts`<br>`apps/mynews/app/(root)/data/runtime-capabilities.ts`<br>`apps/mynews/scripts/check-build-env.mjs` | Supabase project base for the app. Unset makes every reader and composer surface render the honest not-connected state. check-build-env.mjs rejects a release build without it. |
| `EXPO_PUBLIC_MYNEWS_WEB_REPORTING_ENABLED` | optional | public | `apps/mynews/app/(root)/data/runtime-capabilities.ts` | Affirmative flag telling the app that the website accepts reports, used for in-app reporting copy. |
| `EXPO_PUBLIC_REVENUECAT_API_KEY` | optional fallback | public | `apps/mynews/app/(root)/data/launch-environment.ts`<br>`apps/mynews/app/(root)/data/runtime-capabilities.ts` | Suite-wide RevenueCat key fallback, last in the resolution order after the MyNews-specific names. |
| `MYNEWS_RELEASE_MANIFEST_PATH` | optional | public | `apps/mynews/scripts/check-build-env.mjs` | Overrides the release-manifest path check-build-env.mjs reads. Defaults to apps/mynews/release-manifest.json. Used by tests and by an operator checking a candidate manifest, not in normal builds. |

## Platform-injected variables (do not set these by hand)

These come from the platform, not from an operator. `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, and `SUPABASE_ANON_KEY` are injected into every edge function runtime by Supabase, and `supabase secrets set` refuses the reserved `SUPABASE_` prefix, so there is nothing to configure. `NODE_ENV` comes from the Next.js build, `EXPO_OS` from the Expo runtime, and `EAS_BUILD_*` from EAS Build. If one of these looks wrong at runtime, the problem is the project link, the build profile, or the deployment, not a missing secret.

| Variable | Injected by | Read by MyNews source | Notes |
| --- | --- | --- | --- |
| `SUPABASE_URL` | injected by the Supabase edge runtime | yes | Project REST base the mynews store, payments store, and worker-run recorder concatenate onto /rest/v1. |
| `SUPABASE_SERVICE_ROLE_KEY` | injected by the Supabase edge runtime | yes | Service-role credential for every mynews edge write and RPC call. Bypasses RLS, so it never leaves the function runtime. |
| `SUPABASE_ANON_KEY` | injected by the Supabase edge runtime | no direct read | Publishable key the gateway uses. No mynews edge source reads it directly; clients send it as the apikey header. |
| `NODE_ENV` | set by the Next.js build and runtime | yes | Set by the Next.js build and runtime, not by an operator. apps/mynews-web/middleware.ts relaxes one security header outside production based on it. |
| `EXPO_OS` | injected by the Expo runtime | yes | Injected by Expo Router at runtime. runtime-capabilities.ts reads it to pick the ios or android RevenueCat key. Not operator-set. |
| `EAS_BUILD_PROFILE` | injected by EAS Build | yes | Injected by EAS Build. apps/mynews/scripts/check-build-env.mjs enforces the full release env contract only when this is the production profile, and reports skipped otherwise. A local run with it unset therefore checks nothing, which is why the gate belongs in the EAS build step. |
| `EAS_BUILD_PLATFORM` | injected by EAS Build | yes | Injected by EAS Build. Tells check-build-env.mjs which RevenueCat key name (ios or android) must be present and prefix-valid for this build. |

## Operator-only variables (no application source reads them)

These are set in the shell or CI environment that runs the release scripts. They are listed by hand because no scan root reads them, and they are part of the release checklist all the same. `scripts/mynews-smoke.sh` additionally reads `MYNEWS_FUNCTIONS_URL`, which is documented in the website table above because the site reads it too.

| Variable | Required | Secret or public | Purpose |
| --- | --- | --- | --- |
| `SUPABASE_PROJECT_REF` | required for deploy | public | Supabase project ref that scripts/mynews-deploy.sh links and deploys against. |
| `SUPABASE_ACCESS_TOKEN` | required for deploy | secret | Supabase CLI personal access token used by scripts/mynews-deploy.sh and mynews-rollback.sh. |
| `MYNEWS_SMOKE_ANON_KEY` | required for smoke | public | Publishable anon key scripts/mynews-smoke.sh sends as `apikey` so the gateway reaches the function and returns the real 401 from the JWT gate instead of a gateway-level rejection. |
