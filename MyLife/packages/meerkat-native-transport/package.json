{
  "name": "@mylife/meerkat-native-transport",
  "version": "0.1.0",
  "private": true,
  "description": "Owned Expo native module for Meerkat's Nearby (Multipeer / Wi-Fi Direct) data transport and BLE wake-only rung. TS bridge + config plugin + native scaffolding; returns null when the native side is absent so rungs stay honestly unavailable in Expo Go.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "react-native": "./src/index.ts",
  "sideEffects": false,
  "expo": {
    "platforms": ["ios", "android"]
  },
  "files": [
    "src",
    "ios",
    "android",
    "app.plugin.js",
    "expo-module.config.json"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --passWithNoTests"
  },
  "peerDependencies": {
    "expo": "~54.0.33",
    "react-native": "~0.81.5"
  },
  "peerDependenciesMeta": {
    "expo": { "optional": true },
    "react-native": { "optional": true }
  },
  "devDependencies": {
    "@mylife/typescript-config": "workspace:*",
    "typescript": "^5.7.0",
    "vitest": "^3.2.6"
  }
}
