# DoWork production audit, 2026-07-04

Branch: `feature/dowork-trainer-launch`  
HEAD audited: `495fb04b7274f6e7617da8e3a2a03e06719106a0`  
Worktree: `/Users/trey/Desktop/Apps/MyLife/.claude/worktrees/dowork-trainer-launch`  
Mode: report-only. No source code was changed.

## Inputs read

- Product spec: `docs/plans/queue/36-dowork-trainer-platform-launch.md`
- App review notes: `apps/dowork/Tickets/app-review-notes.md`
- Supabase runbook: `docs/runbooks/dowork-supabase-setup.md`
- Prior reviews: `docs/sessions/2026-07-04-dowork-production-review.md`, `docs/sessions/2026-07-04-dowork-punch-list-wave.md`
- Error ledger: `errors_log.md` rows dated `2026-07-04`
- Scope reviewed: `apps/dowork`, the six `supabase/functions/dowork-*` functions, consumed `modules/workouts` surfaces, and `scripts/check-dowork-parity.mjs`

Known deferred items from the prompt were excluded from findings: two-brand visual seams on ported screens, near-zero a11y on the ported half, builder-first first-workout funnel, trainer-platform block-list read filtering, the deep RevenueCat price fallback, Expo SDK hoist skew, and device-bound voice, GPS, push, purchase, and hardware QA.

## Verification

- `cd apps/dowork && npx vitest run --maxWorkers=2`
  - PASS: 26 test files, 375 tests
- `node scripts/check-dowork-parity.mjs`
  - PASS: all DoWork parity checks passed

These gates prove the current automated surface is stable. They do not cover several user-facing and server-contract failures below.

## Verdict

Production readiness: 6.8 out of 10.

DoWork is much closer than the prior production review: the required test suite and parity script pass, the six edge functions exist, the main trainer/client/video/coaching routes are wired, and the prior critical queue and sign-in route regressions are mostly closed. It is not production-ready for public launch yet because several source-verified issues still break account lifecycle promises, paid entitlement timing, account switching, ended coaching relationships, workout save semantics, and some high-value mobile reliability paths.

## Top 9 explicit gaps

1. Critical: Account deletion skips `dw_purchase_events`, despite the App Review promise that deletion removes all server data.
2. High: Email magic-link and password-recovery callbacks do not establish a Supabase session or complete password reset.
3. High: Deactivated or unverified trainers are hidden from discovery but can still upload and mint playback signed URLs.
4. High: RevenueCat `CANCELLATION` events revoke premium access immediately, even when `current_period_end` is still in the future.
5. High: Ended client links can still receive text feedback, and failed text feedback is always presented as saved offline.
6. High: Offline workout shares can replay under a different account after anonymous-to-email sign-in.
7. Medium: Save Workout and Discard Workout are no-ops for title, notes, visibility, and discard intent.
8. Medium: Continuous voice control can exhaust its restart budget during normal Android silence turnover.
9. Medium: User-facing polish and reliability still have visible gaps: raw errors, Android keyboard behavior, dead Settings/About and workout mic rows, unvirtualized long lists, GPS chart/stats issues, and plan unsubscribe no-op.

## Persona journey matrix

| Persona | Expected function | Result | State coverage and evidence |
|---|---|---|---|
| Trainer | Sign in, recover account, redeem invite, enter Studio | FAIL for email-link and recovery callback, PASS for password path and invite route | Email/password screen exists in `apps/dowork/app/(root)/account.tsx:37`; magic links use the callback redirect in `data/account.ts:45`; callback only waits and routes in `auth-callback.tsx:9`; Supabase URL session detection is disabled in `DoWorkCloudProvider.tsx:136`. Trainer invite success routes to Studio in `redeem-invite.tsx:84`. |
| Trainer | Studio upload, manage videos, edit profile | PASS for happy path, FAIL for deactivated trainer server truth | Studio has cloud-off, not-trainer, error, loading, retry, and tabs in `studio.tsx:102` and `studio.tsx:157`. Upload queue has empty, progress, done, error, and retry states in `UploadQueue.tsx:196` and `UploadQueue.tsx:239`. Server upload only checks trainer row existence in `dowork-upload-finalize/index.ts:241` and `index.ts:508`, not active or verified state. |
| Trainer | Public profile, free and premium library, report/block affordances | PASS for ordinary profile/library, FAIL for server deactivation enforcement | Public profile lookup filters active and verified trainers in `cloud-trainers.ts:160`. Exercise/video rails filter active and verified trainers in `cloud-trainer-videos.ts:217`. Playback edge entitlement does not require active or verified trainer state in `dowork-playback-url/index.ts:142`. |
| Trainer | Client roster, invite, end/reactivate, form-check thread | FAIL for ended-link write semantics | Client cards show ended links and allow form-check thread preview for any non-invited link in `clients.tsx:295`. Form-check review always renders the text/video composer in `form-check/[id].tsx:375`. DB feedback insert uses `dw_form_feedback_participant(form_check_id)` with `p_require_active` defaulting false in migration `20260703000001_dowork_trainer_platform_v2.sql:202` and policy line `489`. |
| Trainer | Review form checks, post feedback, attach timestamp, video reply, notify client | PASS for active-link happy path, FAIL for safety and offline classification | Plan requires feedback and timestamp review at `docs/plans/queue/36-dowork-trainer-platform-launch.md:376`. Video form-check upload is active-gated in `dowork-upload-finalize/index.ts:286` and `index.ts:327`. Text feedback queues every failed text insert if body length is valid in `cloud-coaching.ts:623`, and the screen always says saved offline in `form-check/[id].tsx:237`. Form-check UGC has no report target kind in `cloud-reports.ts:12` or SQL check `20260428000004_dowork_moderation.sql:10`. |
| Trainer | Earnings and subscription counts | PASS source-level | RevenueCat webhook writes events and recomputes subscriber count in `dowork-rc-webhook/index.ts:185`. Earnings reads are covered by `cloud-subscriptions.ts`. The cancellation timing bug below affects entitlement and subscriber count, not route existence. |
| Paid subscriber | Discover trainer, open profile, see free and premium libraries, buy subscription | FAIL for paid-through cancellation semantics, PASS for route/state coverage | Trainer list has loading, error, empty, and search states in `(tabs)/trainers.tsx:166`. Profile separates free and premium libraries in `trainer/[handle].tsx:442` and `trainer/[handle].tsx:476`. Paywall uses store price gating in `PaywallSheet.tsx:91` and `PaywallSheet.tsx:287`. `CANCELLATION` maps to `cancelled` in `dowork-rc-webhook/index.ts:37`, while client and DB entitlement require `status = active` in `cloud-subscriptions.ts:118` and migration line `545`. |
| Paid subscriber | Play premium videos with voice, downloads, errors, and offline fallback | FAIL for voice restart and some error/download states | Playback error screen renders `load.message` directly in `player.tsx:486`; playback data returns machine codes in `cloud-playback.ts:40`, while user copy exists in `friendly-errors.ts:27`. Continuous voice restart cap is `4` per `10_000 ms` in `restart-policy.ts:10`, and Android silence is configured at `2000 ms` in `useVoiceCoach.ts:87`. Download revoke check treats markerless 403 as revoked in `downloads.ts:229` and deletes local files in `downloads.ts:379`. |
| Coaching client | Redeem client invite, open My Trainer, use included premium library, send form checks, read feedback | FAIL for ended-link feedback and private UGC safety exit | Manual and deep-link invite routes exist in `client-invite/index.tsx:28` and `client-invite/[code].tsx:61`. My Trainer link is active-link based per Plan 36 line `372`. Form-check upload sign/finalize requires active link in `dowork-upload-finalize/index.ts:286` and `index.ts:327`. After a relationship is ended, text feedback can still insert because feedback RLS does not require active link. |
| Solo lifter | Onboard, build workout, run session, finish, save or discard | FAIL for Save Workout and session UI reliability | Onboarding routes are present in `onboarding.tsx:36` and `onboarding.tsx:109`. Builder validates and saves in `builder.tsx:343`. Session has loading/error/live/end states around `session.tsx:576` and `session.tsx:713`. On completion it routes to Save Workout with `sessionId` in `session.tsx:434`, but Save Workout `handleSave` and `handleDiscard` only call `router.back()` in `save-workout.tsx:30` and `save-workout.tsx:34`. |
| Solo lifter | Plans, exercises, GPS, social sharing, settings | FAIL for several secondary workflows | Plan unsubscribe passes `activeSub.id` to a helper expecting `planId` in `plans.tsx:63` and `modules/workouts/src/db/crud.ts:954`. Exercises load up to 400 rows and render via ScrollView map in `exercises.tsx:103` and `exercises.tsx:362`. GPS preserves null altitude at mapping time in `position-source.ts:71`, but charts null as zero in `gps.tsx:105` and stores max speed equal to average speed in `gps.tsx:303`. Settings About row is a no-op in `(tabs)/settings.tsx:244`. |

## Findings

### Critical

#### C1. Account deletion leaves raw RevenueCat purchase events

Evidence:

- App Review notes promise account deletion removes all server data in `apps/dowork/Tickets/app-review-notes.md:50`.
- `dw_purchase_events` stores `user_id`, `trainer_id`, product data, price, and raw JSON in migration `20260703000001_dowork_trainer_platform_v2.sql:58`.
- `dowork-delete-account` deletes subscriptions, social rows, reports, videos, blocks, profiles, tokens, and storage, but not `dw_purchase_events` in `supabase/functions/dowork-delete-account/index.ts:80`.
- The delete-account test expectation omits `dw_purchase_events` in `supabase/functions/dowork-delete-account/__tests__/index.test.ts:83`.

Failure scenario:

A user deletes their account. Auth user, visible profile, subscriptions, push tokens, and media are removed, but `dw_purchase_events` still retains the user UUID and raw RevenueCat payload. That conflicts with the in-app and App Review promise unless there is an explicit retention policy and anonymization.

Suggested fix shape:

Decide the policy explicitly. If deletion means deletion, remove rows where `user_id = auth.uid()` and consider trainer-side records where the deleting user owns the trainer. If financial audit retention is required, anonymize `user_id`, scrub personal fields from `raw`, and update App Review copy and tests to state retained purchase ledger data clearly.

Verified/refuted note:

Verified in source. This is not the deferred purchase price fallback. It is account deletion coverage.

### High

#### H1. Email magic-link and password-recovery callbacks do not establish a session

Evidence:

- Magic links and recovery emails are sent with the auth callback redirect in `apps/dowork/app/(root)/data/account.ts:45` and `account.ts:79`.
- `AuthCallback` only checks params, waits 600 ms, and routes away in `apps/dowork/app/(root)/auth-callback.tsx:9`.
- Supabase client has `detectSessionInUrl: false` in `apps/dowork/app/(root)/providers/DoWorkCloudProvider.tsx:136`.
- There is no `exchangeCodeForSession` or `setSession` call under `apps/dowork/app` or `apps/dowork/lib`.

Failure scenario:

A user taps an email sign-in link or password-recovery link. The app opens `dowork://auth-callback`, but no token or code is exchanged into the Supabase client, so the user remains anonymous. Password recovery also lacks a new-password UI after the callback.

Suggested fix shape:

Parse `code`, `access_token`, `refresh_token`, and error params in `auth-callback`. Call the proper Supabase session exchange or setter through the shared client, then route only after `auth.getSession()` confirms the signed-in user. Add tests for magic link, recovery link, error callback, and malformed callback.

Verified/refuted note:

Verified in source. Password sign-in exists and works as a separate path, so this is not the prior "sign-in rows unreachable" issue.

#### H2. Deactivated or unverified trainer content can still upload and play

Evidence:

- Public trainer profile lookup filters `is_active = true` and `is_verified = true` in `apps/dowork/app/(root)/data/cloud-trainers.ts:160`.
- Trainer video rails also filter active and verified trainers in `apps/dowork/app/(root)/data/cloud-trainer-videos.ts:217`.
- Playback entitlement checks hidden, free, owner, subscription, and client-link status, but not trainer active or verified state in `supabase/functions/dowork-playback-url/index.ts:142`.
- RLS select policy has the same omission in `supabase/migrations/20260703000001_dowork_trainer_platform_v2.sql:537`.
- Upload signing only requires a trainer row in `supabase/functions/dowork-upload-finalize/index.ts:241`, and `getTrainerIdForUser` selects only `id` in `index.ts:508`.

Failure scenario:

Moderation deactivates a trainer by setting `dw_trainers.is_active = false`. The profile disappears from discovery, but old video links, deep links, active clients, active subscribers, and free-video paths can still mint signed playback URLs. The trainer can also continue uploading new videos because upload signing ignores active and verified flags.

Suggested fix shape:

Make server truth require `dw_trainers.is_active = true` and `is_verified = true` for public, subscriber, and client-link playback and trainer-video upload/finalize. Decide separately whether owner preview should remain available for inactive trainers, then encode that path explicitly. Add edge tests and RLS tests for inactive and unverified trainers.

Verified/refuted note:

Verified in source. The base four-path entitlement exists, but active/verified trainer state is missing from the server side.

#### H3. RevenueCat cancellation revokes paid-through access immediately

Evidence:

- Webhook comment says cancellation access typically runs to period end in `supabase/functions/dowork-rc-webhook/index.ts:18`.
- `STATUS_BY_EVENT.CANCELLATION` maps to `cancelled` in `dowork-rc-webhook/index.ts:37`, and the upsert writes that status in `index.ts:185`.
- DB entitlement requires `status = 'active'` and future period in `20260703000001_dowork_trainer_platform_v2.sql:545`.
- Client mirror requires `subscription.status === 'active'` in `apps/dowork/app/(root)/data/cloud-subscriptions.ts:118`.
- Existing tests codify `cancelled` as inactive in `cloud-subscriptions.test.ts:77`.

Failure scenario:

A subscriber cancels auto-renewal with paid time remaining. RevenueCat sends `CANCELLATION`; DoWork writes `cancelled`; premium playback disappears immediately even though the user should retain access until `current_period_end`, unless the event is a refund or revocation.

Suggested fix shape:

Preserve access until period end. Either keep status `active` with a cancellation marker, or update entitlement to allow `cancelled` rows with a future `current_period_end`. Use expiration, refund, revocation, or explicit period-end events for immediate access loss. Update webhook tests, client subscription tests, subscriber count semantics, and paywall copy.

Verified/refuted note:

Verified against local code and comments. This is not the deferred store price fallback.

#### H4. Ended client links can still receive text feedback

Evidence:

- Plan 36 makes active client links the entitlement path for trainer/client coaching and form-check uploads in `docs/plans/queue/36-dowork-trainer-platform-launch.md:237` and `:240`.
- Form-check video signing and finalize require an active link in `supabase/functions/dowork-upload-finalize/index.ts:286` and `index.ts:327`.
- `dw_form_feedback_participant` defaults `p_require_active` to false in `20260703000001_dowork_trainer_platform_v2.sql:202`.
- `dw_form_feedback_participant_insert` calls it without requiring active link in migration line `489`.
- Trainer UI opens form-check threads for any non-invited link, including ended links, in `apps/dowork/app/(root)/clients.tsx:295`.
- Form-check screen always renders the composer in `apps/dowork/app/(root)/form-check/[id].tsx:375`.

Failure scenario:

A trainer ends a coaching relationship. Video uploads are correctly blocked, but either participant can still open historical checks and insert new text feedback. That makes ended coaching relationships writable and inconsistent with the active-link upload path.

Suggested fix shape:

Change the feedback insert policy to require active participation, for example `public.dw_form_feedback_participant(form_check_id, true)`. Keep select read-only for historical access if desired. Make `form-check/[id].tsx` render history read-only for ended links, and add RLS/client tests for active, invited, and ended link states.

Verified/refuted note:

Verified in source. Historical read access may be intentional; new writes after end are the defect.

#### H5. Text feedback queues permanent rejects and tells users it saved offline

Evidence:

- `postFormFeedback` queues any failed text insert if the body is non-empty and under length cap in `apps/dowork/app/(root)/data/cloud-coaching.ts:623`.
- The comment says only transport-shaped failures should queue in `cloud-coaching.ts:616`, but no transport/permanent classification exists.
- The UI always says "Saved offline. It will send when you reconnect." and clears the draft on any `!result.ok` in `apps/dowork/app/(root)/form-check/[id].tsx:237`.
- Queue comments acknowledge ended or revoked participant rejects can retry until capped in `cloud-coaching.ts:888`.

Failure scenario:

RLS rejects a feedback insert because the link ended, the participant was revoked, or the form check was deleted. The app tells the user the feedback was saved offline, clears the text, retries up to five times, then drops it.

Suggested fix shape:

Have data functions return a distinct queued result, such as `{ ok: false, queued: true }`. Queue only known transient errors. For permanent server rejects, keep the draft, show friendly actionable copy, and do not enqueue poison items.

Verified/refuted note:

Verified in source. This is related to H4 but still needs a client-side fix even after RLS is tightened.

#### H6. Offline workout shares can replay under a different account

Evidence:

- `PendingShareItem` stores only `input`, created time, attempts, and error in `apps/dowork/app/(root)/data/cloud-shares.ts:89`.
- `uploadWorkoutShareCore` resolves `user_id` from the current Supabase session at replay time in `cloud-shares.ts:248`.
- Queues are cleared on explicit sign-out in `DoWorkCloudProvider.tsx:271`, but password sign-in from anonymous state uses `signInWithPassword` and `refreshIdentity` in `account.tsx:54`, not sign-out.
- Startup and foreground flushes use the current client/session in `DoWorkCloudProvider.tsx:155` and `:223`.

Failure scenario:

An anonymous user queues a workout share while offline. Later, without explicit sign-out, they sign into an email account. The pending share lacks the enqueueing user id, so replay posts it as the email account.

Suggested fix shape:

Store the enqueueing user id on pending share items, as comments, likes, and feedback already do. On flush, post only if the current auth uid matches, otherwise hold, migrate with explicit confirmation, or drop with a local notice. Also clear or re-key all queues on auth user-id transition, not only explicit sign-out.

Verified/refuted note:

Verified for workout shares. Refuted for comments, likes, and feedback because those queues carry `userId` or `authorUserId`.

#### H7. Form-check UGC has no report or block action

Evidence:

- Report target kinds are limited to `share`, `comment`, `trainer_video`, and `profile` in `apps/dowork/app/(root)/data/cloud-reports.ts:12`.
- SQL enforces the same target-kind check in `supabase/migrations/20260428000004_dowork_moderation.sql:10`.
- Form-check review renders the video, feedback rows, and composer without report, block, or end-coaching safety action in `apps/dowork/app/(root)/form-check/[id].tsx:309`.

Failure scenario:

A client receives abusive private video feedback, or a trainer receives abusive client form-check content. The private review thread has no in-context report path, and the database would reject a `form_check` or `form_feedback` report even if the UI tried to submit one.

Suggested fix shape:

Add `form_check` and `form_feedback` target kinds to TS and SQL. Add overflow actions to the form-check screen, scoped to participant role. For block behavior, provide a participant-appropriate path such as end coaching, block account, and report content, then verify App Review notes match the implemented surface.

Verified/refuted note:

Verified as a safety gap for private form-check UGC. Public trainer profile/video report paths exist, so this is not a duplicate of the prior trainer-surface finding.

### Medium

#### M1. Save Workout and Discard Workout do not save or discard user input

Evidence:

- Completed session is written before routing to Save Workout in `apps/dowork/app/(root)/session.tsx:423`.
- The route passes `sessionId` in `session.tsx:434`.
- Save Workout collects title, description, and visibility in `apps/dowork/app/(root)/save-workout.tsx:21`.
- `handleSave` only calls `router.back()` in `save-workout.tsx:30`.
- `handleDiscard` also only calls `router.back()` in `save-workout.tsx:34`.
- `wk_workout_sessions` has no title, notes, or visibility columns in `modules/workouts/src/db/schema.ts:56`.

Failure scenario:

A solo lifter finishes a workout, enters a title and notes, chooses visibility, and taps Save. Nothing is persisted. If they tap Discard Workout, the already-completed local session is not deleted either.

Suggested fix shape:

Define the intended data model. Either persist session title, notes, and visibility on `wk_workout_sessions`, or make Save Workout create/update a cloud workout share when visibility is public. Make Discard delete or mark the just-created session according to product intent. Add tests for save, discard, and back navigation.

Verified/refuted note:

Verified in source. The exercise set completion data is saved before this screen, but the screen-specific user input and discard intent are not.

#### M2. Continuous voice can give up during normal Android silence turnover

Evidence:

- Android complete silence timeout is set to `2000` ms in `apps/dowork/lib/voice/useVoiceCoach.ts:87`.
- Continuous mode restarts after every `end` when `shouldListenRef.current` is true in `useVoiceCoach.ts:201`.
- Restart policy allows only four restarts in a 10 second rolling window in `apps/dowork/lib/voice/restart-policy.ts:10`.
- The fifth restart in that window sets `handsFreeInterrupted` in `useVoiceCoach.ts:195`.

Failure scenario:

In a quiet stretch while a video is playing, Android can emit normal silence `end` events every two seconds. Four normal endings inside 10 seconds use the full rapid-restart budget; the next ending stops hands-free voice and falls back to push-to-talk even though nothing unhealthy happened.

Suggested fix shape:

Separate normal silence turnover from unhealthy restart loops. Reset budget after a healthy recognition lifetime, count only rapid start/error/end failures, or use a much larger silence-turnover budget with backoff. Add policy tests for normal Android silence cycles.

Verified/refuted note:

Verified in source. This is not the prior restart oscillation bug; it is the opposite failure mode.

#### M3. Playback and other key screens still surface raw errors

Evidence:

- Friendly mapper exists in `apps/dowork/app/(root)/data/friendly-errors.ts:20`, including user copy for `not_entitled` and `not_found` in lines `27` and `30`.
- `getPlaybackSource` can return raw machine codes in `apps/dowork/app/(root)/data/cloud-playback.ts:40`.
- Player stores `result.error` directly in `apps/dowork/app/(root)/player.tsx:211` and renders it in `player.tsx:486`.
- Account screen catches and renders raw auth error messages in `apps/dowork/app/(root)/account.tsx:37`.
- Studio upload stores raw upload errors in `apps/dowork/app/(root)/components/studio/UploadQueue.tsx:196`.

Failure scenario:

A user without entitlement opens a premium video and sees `not_entitled`, or a user hits a Supabase/RLS/auth failure and sees backend language instead of actionable app copy.

Suggested fix shape:

Require `friendlyError(...)` at every UI banner, alert, and empty/error state boundary. Keep raw strings in logs only. Add tests for playback entitlement errors, auth errors, upload errors, report/block errors, and invite redemption errors.

Verified/refuted note:

Verified in source. Prior passes adopted friendly errors on some surfaces, but coverage is incomplete.

#### M4. Download entitlement probe deletes local files on markerless 403

Evidence:

- Comment assumes the playback function is the only 403 source in `apps/dowork/app/(root)/data/downloads.ts:180`.
- `checkDownloadEntitlement` treats `status === 403` with `marker === null` as revoked in `downloads.ts:229`.
- `openDownloadedVideo` deletes the file when verdict is revoked in `downloads.ts:379`.

Failure scenario:

Supabase Functions, JWT middleware, a gateway, or CORS-like failure returns 403 with an unreadable or non-JSON body. The app treats it as entitlement loss and removes a valid offline video.

Suggested fix shape:

Only delete when the server response explicitly carries `error: 'not_entitled'` from the playback function. Treat markerless 403 as offline or unknown. Add a markerless 403 test that keeps the file.

Verified/refuted note:

Source-verified resilience risk. The edge function itself returns JSON, but the mobile client is responsible for ambiguous platform-layer failures.

#### M5. Android keyboard avoidance is not actually configured for the form-check composer

Evidence:

- Form-check screen opts into keyboard avoidance in `apps/dowork/app/(root)/form-check/[id].tsx:309`.
- Shared `CoachingScreen` uses `behavior={Platform.OS === 'ios' ? 'padding' : undefined}` in `components/coaching/coaching-kit.tsx:54`.
- `apps/dowork/app.json:30` has no Android soft-input resize setting.

Failure scenario:

On Android builds that do not resize automatically, focusing the bottom feedback composer can leave the input and send/video buttons under the keyboard.

Suggested fix shape:

Set an Android-tested soft-input mode or use an Android keyboard-aware scroll wrapper that scrolls the composer above the IME. Add a device or Playwright-style native screenshot check if available.

Verified/refuted note:

Source-verified risk. Not device-tested in this audit.

#### M6. Push prompt answered state is device-global, not user-scoped

Evidence:

- Prompt state is stored under a single key, `dowork.push_prompt.v1`, in `apps/dowork/app/(root)/components/PushPrompt.tsx:32`.
- Dismissed and registered states are read without user id in `PushPrompt.tsx:46`.
- `shouldOfferPushPrompt` hides the prompt once `state.registered` or `state.dismissed` is true in `PushPrompt.tsx:82`.
- Sign-out removes tokens in `DoWorkCloudProvider.tsx:282`, but prompt state is not cleared or re-keyed.

Failure scenario:

User A dismisses or registers push on a shared device, then signs out. User B signs in; server tokens are absent for B, but the device-global prompt state hides the prompt, so B never sees the launch-critical nudge.

Suggested fix shape:

Key prompt state by user id, or clear prompt state on auth user-id transition and let server token existence be the registered truth.

Verified/refuted note:

Verified in source. This is separate from founder-ops push credential setup.

#### M7. Plan unsubscribe passes the wrong identifier

Evidence:

- Plans screen calls `unsubscribeFromPlan(db, activeSub.id)` in `apps/dowork/app/(root)/plans.tsx:63`.
- `unsubscribeFromPlan` expects a plan id and updates `WHERE plan_id = ?` in `modules/workouts/src/db/crud.ts:954`.
- `PlanSubscriptionRow` has both `id` and `planId` in `modules/workouts/src/types.ts:501`.

Failure scenario:

The user taps Unsubscribe on the active plan. The update matches zero rows because it receives the subscription id, so the plan remains active.

Suggested fix shape:

Pass `activeSub.planId` or change the helper to accept subscription id with a clearer name. Add a UI-level regression test around the Plans unsubscribe action.

Verified/refuted note:

Verified in source. Module tests cover the helper when given a plan id, not this DoWork screen call site.

#### M8. Workout session has stale voice UI, no keep-awake, and a hot interval effect

Evidence:

- `session.tsx` defines a no-op `useKeepAwake` with comment "expo-keep-awake is not installed" in `apps/dowork/app/(root)/session.tsx:69`, while player imports real `expo-keep-awake` in `player.tsx:11`.
- Session calls the stub in `session.tsx:183`.
- Live session mic button shows "Voice controls will be connected in a later pass." in `session.tsx:739`.
- The interval effect depends on full mutable `status` in `session.tsx:323`, and the interval updates `status` every 200 ms in `session.tsx:341`.

Failure scenario:

The screen can auto-lock during an active workout. A user can tap a mic button and get a "later pass" message. During active sessions the interval effect repeatedly tears down and recreates, increasing JS churn and drift risk.

Suggested fix shape:

Use real keep-awake while session is active. Remove or fully implement the session mic button. Key interval lifecycle on stable fields such as session state and store mutable status in a ref or reducer.

Verified/refuted note:

Verified in source. This is not a failure of the Plan 36 video-player voice implementation.

#### M9. Large trainer and exercise lists are unvirtualized

Evidence:

- Studio Manage renders every trainer video via `.map` in `apps/dowork/app/(root)/components/studio/ManageGrid.tsx:208`.
- Trainer profile renders free videos via `.map` in `trainer/[handle].tsx:442` and premium videos via `.map` in `trainer/[handle].tsx:476`.
- Exercises load up to 400 rows in `apps/dowork/app/(root)/exercises.tsx:103` and render them in a grid via `.map` in `exercises.tsx:362`.

Failure scenario:

A real trainer with a large library, or a full exercise catalog on a midrange phone, causes heavy initial render, scroll jank, and image memory pressure.

Suggested fix shape:

Use `FlatList`, `SectionList`, or FlashList for these list surfaces. Memoize row components and section data. Add pagination or Supabase range queries for trainer libraries if libraries can grow unbounded.

Verified/refuted note:

Verified in source. This is a scalability and efficiency finding, not a current test failure.

### Low

#### L1. Settings About DoWork row is still a dead action

Evidence:

- Settings `appRows` includes `About DoWork` with `onPress: () => {}` in `apps/dowork/app/(root)/(tabs)/settings.tsx:244`.
- Rows render as pressable actions in `settings.tsx:365`.

Failure scenario:

A user taps Settings, App, About DoWork. The row depresses but nothing opens.

Suggested fix shape:

Wire it to a real About, legal, or support screen, or remove the row. Add a simple guard against empty settings row handlers.

Verified/refuted note:

Verified in source. Prior notes said dead settings rows were fixed, but this row remains.

#### L2. GPS live tracking performs cumulative work and stores inaccurate max speed

Evidence:

- Each GPS fix inserts one point and re-queries all points in `apps/dowork/app/(root)/gps.tsx:198`.
- Metrics recompute from the full `livePoints` array every update in `gps.tsx:234`.
- `maxSpeedKmh` is stored as the same value as average speed in `gps.tsx:303`.

Failure scenario:

Longer runs do more work per fix, increasing battery and jank risk. Route history underreports peak speed because max speed is not actually derived.

Suggested fix shape:

Append the new point to state, maintain rolling metrics for the live display, and compute final full metrics on stop. Derive max speed from valid `speedMps` samples or segment speed.

Verified/refuted note:

Verified in source. Fake GPS mapping concerns were refuted: `position-source.ts:71` preserves null altitude and invalid speed as null.

#### L3. GPS elevation chart maps unknown altitude to zero

Evidence:

- Location mapping preserves unknown altitude as null in `apps/dowork/lib/gps/position-source.ts:71`.
- Elevation gain calculation filters null altitude in `modules/workouts/src/gps/metrics.ts:70`.
- The live chart maps `point.altitudeMeters ?? 0` in `apps/dowork/app/(root)/gps.tsx:105`.

Failure scenario:

On a device that reports unknown altitude, the chart shows a false drop to 0 m. Mixed known and unknown points skew the chart.

Suggested fix shape:

Build the elevation chart only from altitude-known points, or show an "elevation unavailable" state until enough real altitude samples exist.

Verified/refuted note:

Verified in source. This is a charting issue, not a GPS fix mapping issue.

#### L4. Form-check feedback load failures become false empty states

Evidence:

- Feedback load failure is converted to an empty array in `apps/dowork/app/(root)/form-check/[id].tsx:122`.
- The screen then renders "No feedback yet." in `form-check/[id].tsx:352`.

Failure scenario:

A network, RLS, or token failure hides existing feedback and tells the trainer or client that no feedback exists.

Suggested fix shape:

Track `feedbackError` separately from true empty state. Render a retryable friendly error when feedback cannot load.

Verified/refuted note:

Verified in source.

#### L5. Dead `DoWorkProvider` remains mounted

Evidence:

- `apps/dowork/app/(root)/providers/DoWorkProvider.tsx:17` hardcodes cloud unavailable state.
- It is still mounted in `apps/dowork/app/(root)/_layout.tsx:93`.
- No current consumers were found.

Failure scenario:

No active runtime break today, but a future import of this stale provider can get incorrect cloud and user state despite `DoWorkCloudProvider` being the real implementation.

Suggested fix shape:

Remove it or adapt it to delegate to `DoWorkCloudProvider`.

Verified/refuted note:

Verified as dead-code risk, not a current user-facing bug.

## Refuted or not re-reported

- Base four-path trainer video entitlement exists in playback and RLS: free, owner, active subscription, active client link. The gaps are cancellation timing and active/verified trainer status.
- Form-check video upload caps, MIME checks, and active-link requirements are enforced by `dowork-upload-finalize`.
- The queue double-drain and infinite replay loop is fixed by serialized flushes in `pending-queues.ts:123`.
- Comments, likes, and feedback queue identity drift is mostly refuted because those queued payloads carry `userId` or `authorUserId`. Workout shares are the remaining identity gap.
- Explicit sign-out clears queues before the next session. The remaining issue is anonymous-to-email sign-in without explicit sign-out.
- Push notification route resolution is source-covered for warm and cold taps. Device delivery remains in the known deferred hardware/founder-ops bucket.
- Paywall visible price truth is handled at the sheet layer. The known deep `purchases.ts` fallback remains deferred and was not re-reported.
- GPS fake-location concerns were refuted. Simulator mode is development-gated, and real location mapping does not fabricate speed or altitude.
- The prior voice restart oscillation was not found. The new voice finding is restart-budget exhaustion during normal silence turnover.

## Fix wave recommendation

1. Close account lifecycle first: auth callback, password recovery, account deletion purchase-event handling, and tests.
2. Close paid/server truth next: cancellation semantics, active/verified trainer enforcement, and entitlement tests.
3. Close coaching state correctness: ended-link feedback RLS, read-only ended threads, feedback queue classification, and form-check report targets.
4. Close account switching/offline queue identity for workout shares.
5. Close user-visible workflow defects: Save Workout, plan unsubscribe, raw error boundaries, Android keyboard, and dead rows/buttons.
6. Close efficiency and GPS cleanup: list virtualization, session timer, keep-awake, GPS rolling metrics, max speed, and elevation chart.

