// Progress engine — reading speed, progress tracking, timeline

export { updateProgress, getReadingSpeed, getProgressTimeline } from './progress-engine';
export type { ReadingSpeed, ProgressTimelineEntry } from './types';
