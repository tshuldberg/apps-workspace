# Errors Log Archive (pruned 2026-07-30)

Resolved rows dated 2026-07-28 and earlier, moved from the repo-root errors_log.md per the ~150-row archive rule. Unresolved and Mitigated rows were never archived.

| Date | Error / Symptom | Context (file, command, or module) | Resolution | Status |
|------|-----------------|------------------------------------|------------|--------|
| 2026-07-12 | Yearn boost verifier CRITICAL bypass caught in dual review: `verifyCertificateChain` only checked link-signs-next + termination at embedded Apple Root CA-G3, never read X.509 extensions. G3 anchors all Apple public PKI, so any attacker-controlled G3-rooted leaf could sign a forged StoreKit payload and self-grant free boosts (revenue leak not actually closed). Also sandbox txns granted real prod boosts. | `supabase/functions/yearn-boost-activate/verify.ts` (verifyCertificateChain, parseCertificate); plan 47 Phase 2 | Fix on `feature/yearn-plan47-phases-2-5`: enforce Apple StoreKit chain policy (leaf ext OID 1.2.840.113635.100.6.11.1, intermediate ext OID 1.2.840.113635.100.6.2.1, intermediate CA:TRUE, exactly-3 chain, presented root byte-equals trusted root), reject non-production env, bind to caller via appAccountToken + dedup on per-purchase transactionId. Caught before phase close by opus adversarial review. | Resolved |
| 2026-07-14 | Hosted-service bin crashed on EVERY boot: ready-log referenced block-scoped `storageApiHandler` (ReferenceError) | packages/meerkat-relay/bin/meerkat-hosted-service.mjs (Plan 41 WP-41G) | Fable let-hoisted the handler beside `server`; caught by real-bin process-boundary tests that the codex sandbox cannot run (listen EPERM); landed in `a89ccc61` | Resolved |
| 2026-07-14 | Pre-commit hook failed with a Node module-not-found crash: staging shared `__tests__` dirs wholesale pulled another in-flight packet's tests while their sources were unstaged, and the hook's scope-stash orphaned the imports | .husky/pre-commit + git staging (Plan 41 WP-41B2/E landing) | Precise per-file staging when two packets interleave in one directory; both packets committed clean (`95e29b72`, `9167a2a7`) | Resolved |
| 2026-07-12 | pnpm install ELOOP in fresh checkouts/worktrees: node_modules and apps/yearn/node_modules materialized as self-loop symlinks; a worktree install followed the root symlink into the main checkout and wiped its real node_modules | pnpm install, repo root + apps/yearn | Root cause: 0b7358ec accidentally committed both symlinks with absolute-path targets. Untracked in b317711a (squash of fix/workouts-hub-calculator-parity); gitignore already covers the paths. Main tree reinstalled and reconciled with --frozen-lockfile. Pre-b317711a worktrees still carry the loops. | Resolved |
| 2026-07-12 | 5 (books) vitest suites failed to collect in apps/mobile: rollup parse error on expo-linear-gradient/build/LinearGradient.js (raw JSX in .js) when vi.mock forces its transform | apps/mobile app/(books)/__tests__/{search,add-book,settings,stats,library}.test.tsx | Pre-existing on clean main; blocked the apps/mobile function gate. Aliased expo-linear-gradient to a children-rendering CJS stub in vitest.config.ts (lucide precedent), merged in b317711a. Full mobile suite 58/58 files, 185/185 tests; gate green. | Resolved |
| 2026-07-11 | Supabase migration version collision: bestchef 20260711000001/02 vs dowork same versions on parallel branches; duplicate versions would break `supabase db push` after both merged | supabase/migrations, feature/bestchef-remediation vs feature/dowork-production-readiness | Renumbered dowork's to 20260711000012/13 pre-merge with reference updates (`3cec60b3`); verified 12/13 free on every branch | Resolved |
| 2026-07-11 | mynews suggestion screen used `ensureTermsAccepted` inside SuggestionBody while the hook was destructured only in SuggestionScreen (TS2304 x6); reviewer missed it by typechecking only modules/mynews | apps/mynews/app/(root)/suggestion/[id].tsx | Caught by the pre-commit function gate; hook call moved into SuggestionBody (`549a461b`) | Resolved |
| 2026-07-11 | Merge integration: two parallel DoWork fix waves diverged on RevenueCat CANCELLATION semantics ('cancelled'+paid-through checks vs 'active'+period-end expiry) and duplicated the session-meta writer under two names | supabase/functions/dowork-rc-webhook, modules/workouts | Unified in merge `9a2b485c`: CANCELLATION->active write model (verified compatible with the 20260704000002 RLS paid-through policy incl. refunds), UNCANCELLATION kept, annotateWorkoutSession canonical; suites 559+559 green | Resolved |
| 2026-07-11 | bestchef merge commit gate failed: expo-camera/expo-notifications/@sentry/react-native unresolved because worktree node_modules predated the merged lockfile; first `pnpm install` silently aborted at an interactive purge prompt | .claude/worktrees/main-merge, husky pre-commit | `CI=true pnpm install --no-frozen-lockfile` then recommit; gate green (`b649241d`) | Resolved |
| 2026-07-11 | push-providers.ts web-push fetch body failed DOM-lib typecheck at a second call site (Uint8Array view not BodyInit) after merging the live Plan 42 branch tip | packages/meerkat-relay/src/push-providers.ts:621 | Applied the branch's own 0d85b12f ArrayBuffer-copy pattern in merge `7e47355a`; relay + web typecheck green | Resolved |
| 2026-07-11 | Tracked `node_modules` symlinks (root + apps/yearn) committed in 0b7358ec made the main tree's node_modules self-referential (ELOOP) and broke pnpm in every worktree | `.gitignore` `node_modules/` pattern matches directories only, not symlinks; symlinks were git-added in a scratchpad yearn worktree and rode the merge to main | Untracked and removed both symlinks, added a slash-less `node_modules` ignore rule (81d72b70 on feature/yearn-boost-integrity); reinstalled main tree and worktree with frozen lockfile | Resolved |
| 2026-07-04 | P0 stored XSS -> E2EE key exfiltration (web): openAttachment opened blob: URLs with sender-signed mimeType verbatim, no CSP; a text/html attachment could execute on the app origin and read the secrets IndexedDB store (recovered from stash@{6} of deleted branch feature/meerkat-launch-finish) | apps/meerkat-web/src/ui/InChannelFileCard.tsx | Fixed on main: isInlineRenderSafeMimeType allowlist + honest View/Save paths | Resolved |
| 2026-07-04 | P1 unauthenticated community-node DoS x2: readBody defaulted to Infinity on register/publish/append; challenge route grew an unbounded communities map (recovered from stash@{6}) | packages/meerkat-relay/src/community-node-http.ts | Fixed on feature/meerkat-production-readiness (merged `7e47355a`): per-route bodyLimits caps + eviction | Resolved |
| 2026-07-11 | draft-flush.test.ts: empty-string draft reps parsed to 0 instead of falling back to programmed reps (BH-1) | apps/dowork/lib/workouts/draft-flush.ts:70 | `??` only falls back on null/undefined; empty string passed through. Fixed with `draft?.reps?.trim()` length check before falling back to `exercise.reps`. | Resolved |
| 2026-07-11 | Codex CLI hit account usage limit mid-session; in-flight bulk work rerouted to Claude agents | plan-45 remediation session, `codex exec` | Fallback per global model policy; credits reset same day | Resolved |
| 2026-07-11 | mynews-review suite failed to resolve `@mylife/sync` when swept into the bestchef module test glob | `pnpm --filter @mylife/bestchef test` | Module suite scoped to BestChef paths (audit L10); dowork fn tests re-homed to modules/workouts | Resolved |
| 2026-07-11 | bc_job_health() silently dropped quota-engine fields through two successive create-or-replace copies | supabase migrations 20260704000002 / 20260711000005 | Migration 20260711000008 restores the strict field union with drift history documented (`7f1f0c87`) | Resolved |
| 2026-07-11 | Sentry Expo plugin source-map upload phase would fail the first credential-less production EAS build | apps/bestchef/app.json, plan-45 item 2.2 | Caught in review pre-commit; disableAutoUpload default + documented founder opt-in (`49dbf75c`) | Resolved |
| 2026-07-09 | Meerkat function-gate typecheck lacked declarations for the dependency-free EAS `.mjs` environment guard | `apps/meerkat/scripts/__tests__/check-build-env.function-gate.test.ts`; `pnpm --filter @mylife/meerkat-app typecheck` | Added a matching `.d.mts` public contract for `checkBuildEnv`; mobile and web typechecks now pass | Resolved |
| 2026-07-09 | Meerkat app test typecheck could not resolve the non-exported `@mylife/db/testing` path | `apps/meerkat/app/__tests__/expo-blob-store.test.ts`; `pnpm --filter @mylife/meerkat-app typecheck` | Imported the existing test database helper from the package's public `@mylife/db` barrel and used its adapter; mobile typecheck and focused tests now pass | Resolved |
| 2026-07-09 | Initial Meerkat RevenueCat remediation used the public device key as App User ID, allowing anyone who learned that key to query its CustomerInfo through RevenueCat's public SDK | `apps/meerkat/app/(root)/data/app-unlock.ts`; adversarial pre-commit review | Replaced it with a stable unguessable Ed25519 signature identifier, server-verifies its binding to the authenticated device, and added forgery and transfer-safe tests | Resolved |
| 2026-07-09 | Meerkat function quality gates failed TypeScript rootDir checks because the scaffolder generated imports of the root test helper from package-local tests | `pnpm gate:function --file packages/sync/src/protocol/hosted-auth.ts`; `packages/meerkat-relay/src/__tests__/*.function-gate.test.ts` | Repointed Sync to its established package-local helper and added the same package-local helper for Meerkat Relay; all three affected file gates now pass | Resolved |
| 2026-07-09 | A public submit rejected by the shared per-IP limiter popped a valid single-use humanity token locally even though the node rejected before redeeming it, eventually forcing unnecessary reverification. | `packages/meerkat-relay/src/community-node-http.ts`; mobile/web `public-post-client.ts` | Added additive `humanityTokenConsumed:false` metadata only to the pre-redemption submit 429; both clients restore only on that explicit marker while ambiguous/post-redemption 429s remain spent. Added relay/mobile/web regressions and parity lock. [Session log](docs/sessions/2026-07-09-meerkat-humanity-token-rate-limit-fix.md). | Resolved |
| 2026-07-09 | The $4.99 private-use unlock is not enforced on the private features it claims to sell. | Mobile/web entitlement checks; Blackglass audit C-01 | Added centralized mobile path and web action/pane gates, fail-closed boot revalidation, unpaid deep-link/browser tests, and provider mounting controls. Full mobile/web suites and six browser E2E paths pass. [Audit](docs/reports/REPORT-meerkat-blackglass-production-adversarial-audit-2026-07-09.html). | Resolved |
| 2026-07-09 | Web billing uses a hosted entitlement where the server requires device-signed auth, and hosted/Commons APIs omit browser CORS. | `AppUnlockSection.tsx`, `hosted-auth.ts`, `hosted-api.ts`, `community-node-http.ts` | Rewired web billing and cross-rail redemption to device-signed authorization, added exact-origin API CORS and public bounded relay-health CORS, and verified the flow in real-relay browser E2E. | Resolved |
| 2026-07-09 | A valid mobile StoreKit/Play unlock cannot establish the hosted purchase proof needed for public posting or cross-rail linking. | `public-post-client.ts`, `hosted-receipt-validator.ts`, `meerkat-hosted-service.mjs` | Added a device-bound RevenueCat App User ID, server provider validation, single-use authenticated link redemption, recipient-bound signed grants, and refund revalidation on both rails. | Resolved |
| 2026-07-09 | Production build guard reports sane with all launch-critical Meerkat service and purchase variables unset. | `apps/meerkat/scripts/check-build-env.mjs`; executable audit proof | Production builds now require both RevenueCat keys, HTTPS service/legal URLs, WSS relay, pinned humanity and Commons keys/topics, and secure TURN configuration. The production-profile proof and iOS/Android export pass. | Resolved |
| 2026-07-09 | Stripe webhook replay/out-of-order delivery can overwrite a newer inactive purchase with an older active state. | `hosted-api.ts`, `hosted-billing-store-file.ts`; executable audit proof | Persisted provider event IDs/timestamps and serialized monotonic updates under cross-process file leases; duplicate/stale events are rejected and same-second denial wins. Refund and dispute revocation tests pass. | Resolved |
| 2026-07-09 | Expo Doctor failed 6 of 18 categories, including invalid config, missing `expo-font`, duplicate native modules, New Architecture risks, and SDK drift. | `npx expo-doctor`; `apps/meerkat` | Aligned the Expo dependency graph and config; Expo Doctor 1.20.0 now passes 18/18 and both iOS and Android exports complete. Signed-device/store evidence remains an operator release requirement, not a Doctor failure. | Resolved |
| 2026-07-09 | Public UGC has reporting and moderation backends but no in-app privacy link, Terms acceptance, or block-user surface. | Meerkat mobile/web store-readiness audit | Added mobile/web legal and safety surfaces, versioned Terms acceptance, persona blocking, and a Terms version bound into every signed public post and enforced by the server. Store-review evidence and staffed operations remain operator requirements. | Resolved |
| 2026-07-09 | Public-persona deletion/export UI promises post removal and complete post/reply export, but the deployable persona service only deletes/exports the registry row; the full GDPR coordinator is test-only. | `meerkat-persona-service.mjs`, `persona-service-http.ts`, `gdpr-deletion.ts`; Blackglass C-06 | Composed the coordinator into the deployable persona service with durable community, billing, and console stores; added export and deletion E2E, a durable write barrier, and an in-flight post race regression. | Resolved |
| 2026-07-09 | “Delete my data” leaves mobile `meerkat/blobs` and web IndexedDB `blobBytes` intact, then can destroy the persona key needed for remote deletion. | Mobile Settings deletion; web `MeerkatProvider.deleteMyData`; Blackglass C-07 | Reordered remote deletion before local destruction, clears and verifies raw byte stores, awaits and verifies key deletion, retains references on failure, and recreates identity only after the wipe succeeds. | Resolved |
| 2026-07-09 | Stateful Meerkat production containers built but crashed before readiness under Node ESM or production-only dependencies. | `Dockerfile.platform`; `hosted-auth.ts`; real hosted-service container boot | Replaced the incompatible named CommonJS import, made React runtime dependencies explicit for barrels that export hooks, moved `tsx` to production dependencies, removed runtime package-manager downloads, used production-only installs, and added a CI image boot probe. Hosted and relay containers now boot from the built image. | Resolved |
| 2026-07-09 | Browser relay status reported Unreachable against a healthy real relay because `/healthz` lacked cross-origin access. | Six-path Playwright run with `VITE_MEERKAT_DEFAULT_RELAY_URL`; `packages/meerkat-relay/src/server.ts` | Added wildcard CORS only to the bounded credential-free health endpoint, asserted it in server tests, and reran all six browser paths against the real relay successfully. | Resolved |
| 2026-07-09 | New Meerkat browser regressions initially used an ambiguous Unlock locator and exposed the missing relay-health CORS contract. | `apps/meerkat-web/e2e/launch-paths.spec.ts`; first six-path real-relay run | Scoped the locator to the exact control, fixed the product CORS defect, and reran all six E2E paths green. | Resolved |
| 2026-07-08 | Meerkat parity script failed with `Identifier 'mobileSettingsScreen' has already been declared` after adding own-device Settings guards. | `pnpm check:meerkat-parity`; `scripts/check-meerkat-parity.mjs` | Removed the duplicate local declaration and reran Meerkat parity plus the full `pnpm check:parity --quiet` chain successfully. | Resolved |
| 2026-07-08 | New Meerkat Plan 40 parity guard failed because it assumed both platforms used a `RequestAgain` component name. | `scripts/check-meerkat-parity.mjs`; `pnpm check:meerkat-parity` | Corrected the guard to check mobile `FileIndexRequestButton` and web `RequestAgainAction`; rerun passed all Meerkat parity checks. | Resolved |
| 2026-07-08 | Meerkat web launch E2E failed on first runs: Playwright config used ESM-incompatible `__dirname`, then onboarding selectors targeted duplicate visible labels. | `apps/meerkat-web/playwright.config.ts`; `apps/meerkat-web/e2e/launch-paths.spec.ts`; `pnpm --filter @mylife/meerkat-web test:e2e` | Switched Playwright config to `fileURLToPath(new URL('.', import.meta.url))`, scoped onboarding selectors to the welcome dialog, and scoped the community-name assertion to `main`; rerun passed 3 browser paths with relay path honestly skipped because `MEERKAT_E2E_RELAY_URL` is unset. | Resolved |
| 2026-07-06 | Live unclaimed PROD invite codes committed in repo (trainer code auto-mints a verified live trainer on redeem) | `apps/dowork/Tickets/app-review-notes.md` + `dowork-redeem-invite` | Rotated in prod via Management API (trainer invite expired, client link ended, fresh code minted -> keychain "DoWork trainer invite (rotated 2026-07-06)"); literals scrubbed from docs (`01359c34`). | Resolved |
| 2026-07-06 | 2026-07-05 portfolio ruling wrongly marked the dp-audit fix wave as merged; feature/dowork-trainer-launch was 11 commits ahead of main incl. 2 migrations ALREADY APPLIED to prod (main behind deployed schema) | `git cherry main feature/dowork-trainer-launch` | Branch merged to main `0bc4152f` + pushed 2026-07-06; memory.md session-row claim corrected in place. | Resolved |
| 2026-07-06 | **P3 moderation un-hide (D.2).** Reported content hid only while its row was `status='active'`; the only resolution path `markSafetyActionReviewed` set a non-active status, un-hiding it. No review-and-uphold option. Device-local (`cm_safety_actions` absent from `packages/sync`), so bounded to the reviewing device. | `apps/meerkat-web/src/lib/community-safety.ts` + mobile twin `apps/meerkat/app/(root)/data/community-safety.ts` | Decoupled hide-state from review lifecycle: `isCommunityContentReportHidden` + `listOwnerReviewItems` now hide on `status IN ('active','reviewed')`; only an explicit `'dismissed'` (owner "Un-hide for me") un-hides. Added "Reviewed — still hidden" pill + Un-hide action on both surfaces; bug-encoding test rewritten. Parity-locked. | Resolved |
| 2026-07-07 | DoWork share insert RLS or validation failures were treated as transient, showing "Saved offline" before dropping at retry cap. | `apps/dowork/app/(root)/data/cloud-shares.ts`; user report referencing missing 2026-07-05 open row | Added a shared cloud write failure classifier; shares now queue only no-session, network, timeout, and 5xx failures, while permanent insert verdicts surface to callers. Coaching already had transient-only queueing and now delegates to the shared classifier. | Resolved |
| 2026-07-06 | Meerkat EAS export broken by TWO Metro blockers: dynamic require(name) loops in ble/nearby backends (InvalidRequireCallError) AND expo-router bundling sibling *.test.ts files (node:zlib import crash) | `apps/meerkat/app/(root)/data/ble-backend.ts:99`, `nearby-backend.ts:66`, `apps/meerkat/metro.config.js` | Fixed 2026-07-06 (`62aa86a9`): static optional requires + resolver.blockList for test files; verified with a real successful `expo export` (9.08MB bundle). | Resolved |
| 2026-07-06 | Plan 39 P6 review: receipt HLC could land BEHIND a reader page cursor (snapshot event HLC ahead of node clock), hiding accepted posts forever behind strict hlcAfter | `packages/meerkat-relay/src/community-node.ts` submitPublicPost HLC assignment | Fixed same session: receipt HLC floors on the channel's max snapshot-event HLC via nextHlc; regression test in community-node-public-submit.test.ts. Caught by codex review before commit. | Resolved |
| 2026-07-06 | Plan 39 P6 review: app-unlock proof token was transferable (any purchaser could mint proofs for arbitrary public persona keys, bypassing the $4.99 gate per persona) | `packages/entitlements/src/meerkat-app-token.ts`, `packages/meerkat-relay/src/hosted-api.ts` mint route | Fixed same session: persona proof-of-possession at mint (Ed25519 over appUnlockBindingMessage), one-purchase-one-persona sticky binding (atomic first-bind-wins), submit gate requires binding hash-match with the session persona. Caught by codex review before commit. | Resolved |
| 2026-07-06 | Plan 39 P7 review: postPolicy/postNodeKeyHex were signed into descriptors but not persisted in cm_publications, so takedown/revision reconstruction would break the owner signature for policy-bearing publications | `apps/meerkat(+web) public-publish.ts` persistPublicationRow/reconstructGenesis | Fixed same session: post_policy + post_node_key_hex columns (ensureColumn migration both surfaces), persisted + re-attached in reconstruction. Caught by codex review before commit. | Resolved |
| 2026-07-06 | Plan 39 P12 review: operator unsuspend racing a GDPR delete could clear the delete's revocation, re-validating a deleted account's unexpired session bearers | `packages/meerkat-relay/src/persona-registry.ts` suspend/unsuspend/deleteAccount | Fixed same session (`758e04cf`): per-persona promise lock serializes the revocation lifecycle + post-unrevoke row recheck restores revocation if the row vanished; race test with a delayed store in operator-console-store-file.test.ts. Caught by codex review before commit. | Resolved |
| 2026-07-06 | Plan 39 P12 review: FileOperatorConsoleStore could lose a triage decision (concurrent read-modify-write of triage.json) and swallow the first post-restart audit row after a torn tail (crash mid-append, no trailing newline) | `packages/meerkat-relay/src/operator-console-store-file.ts` | Fixed same session (`758e04cf`): serialized triage write chain + torn-tail newline repair before the first append; regression tests added. Caught by codex review before commit. | Resolved |
| 2026-07-06 | Operator console HTTP tore down connections on error responses sent mid-upload (401/405/413 became client-side socket hang up / ECONNRESET instead of the status) | `packages/meerkat-relay/src/operator-console-http.ts` readBody/error paths | Fixed same session (`758e04cf`): bounded drain-to-end before error responses; adversarial tests then observed real statuses. Caught by the new test suite. | Resolved |
| 2026-07-07 | Plan 39 reconciliation (Track D2): the community-node bin appended `/persona/session/verify` to SESSION_VERIFY_URL while the persona-service doc + P12 session log told operators to set it to `<base>/persona`, doubling the segment to `/persona/persona/session/verify` and failing every public submit closed on a healthy persona service | `packages/meerkat-relay/bin/meerkat-community-node.mjs` session verifier URL | Fixed same session (`b6b7ecc1`): extracted `personaSessionVerifyEndpoint()` (pure, in persona-service-http, barrel-exported) that normalizes off a trailing `/persona` + trailing slashes so both configured forms resolve to `<base>/persona/session/verify`; corrected the docs; guard test persona-session-verify-endpoint.test.ts. Caught by codex review of the reconciliation merge. | Resolved |
| 2026-07-07 | Plan 39 P13 codex review (3 real correctness bugs in the legal pipelines): (1) NCMEC console export marked records exported but the page discarded the NDJSON payload -> queued CSAM evidence could leave the queue undelivered; (2) DISMISSING a csam report still enqueued an NCMEC record (checked reason, ignored status) -> false filing work; (3) a DMCA takedown with unresolved items (URLs/unknown ids) was marked `actioned`, dropping unresolved legal work out of the received queue | `packages/meerkat-relay/src/operator-console-page.ts` (NCMEC export), `operator-console.ts` reviewReport + dmcaTakedown, `dmca-intake.ts` recordTakedown | Fixed same session before commit: (1) export button now downloads the NDJSON blob before marking exported; (2) enqueue gated on status==='reviewed'; (3) recordTakedown keeps the claim `received` (still actionable) + persists `unresolvedItems` when anything is unresolved, only `actioned` when fully resolved. Regression tests added to plan39-legal-pipelines.test.ts. Caught by codex review --uncommitted. | Resolved |
| 2026-07-07 | Plan 39 consolidated review: `VERIFY_TO_VIEW_PRICE_LINE` hardcoded `$4.99` (NC-P5 violation) while the composer sourced the price from billing-config, so a founder-ops price change would show two prices for the same SKU | `apps/meerkat(+web)/.../verify-to-view.ts`, `scripts/check-meerkat-parity.mjs` | Fixed same session `03d81df0`: both twins interpolate the billing-config price (imported directly from @mylife/billing-config, NOT via app-unlock, to keep react-native out of the vitest transform graph); parity lock split to the two static halves. Caught by the adversarial close review. | Resolved |
| 2026-07-07 | Plan 39 close: S12 report sheet was incomplete - the persona-signed report client (public-report.ts, NC-P2) existed mobile-only and was NOT wired into any UI; web had no client twin; the only wired report path was the Plan-19 reader's device-signed createPublicAbuseReport (an NC-P2 concern on a public-tier action). Surfaced by a sibling agent, verified against live git. | `apps/meerkat(+web) public-report.ts`, `public/post/[postId].tsx`, `PublicThreadView.tsx` | Fixed same session `69742ea5`: added web client twin + test, wired a persona-signed Report affordance + reason sheet into the thread (post + reply) on both surfaces, parity-locked. My earlier "complete" call had missed this. | Resolved |
| 2026-07-07 | Main merge pre-commit gate could not resolve newly merged Meerkat deps in the separate main worktree | `.claude/worktrees/main-merge`, `git commit --no-edit`, `pnpm gate:function:changed --staged` | Ran `pnpm install --frozen-lockfile` in `.claude/worktrees/main-merge`; reran `pnpm gate:function:changed --staged` successfully. | Resolved |
| 2026-07-07 | Main merge pre-commit hook failed restoring its temporary stash after the staged Meerkat gate passed, leaving a mode-only add/add conflict on the persona-service bin. | `.claude/worktrees/main-merge`, `git commit --no-edit`, `packages/meerkat-relay/bin/meerkat-persona-service.mjs` | Resolved the mode-only conflict by preserving the executable bit and restaging the file; the hook-created stash was left intact for audit instead of being force-dropped. | Resolved |
| 2026-07-09 | Relay typecheck rejected the Plan 44 schema-state test double because its generic query signature required one implementation to satisfy every possible row subtype. | `packages/meerkat-relay/src/postgres/schema-guard.ts`, `pnpm --filter @mylife/meerkat-relay typecheck` | Narrowed the query contract to the record shape the schema guard actually consumes; focused tests and relay typecheck then passed. | Resolved |
| 2026-07-09 | PostgreSQL operator bins failed from the repository root, and the first synchronous loader could orphan a live migration when its wrapper received a termination signal. | `packages/meerkat-relay/bin/meerkat-postgres-*.mjs`, Codex verification review | Added an executable asynchronous Node wrapper that resolves the relay package's local `tsx/cli`, forwards stdio, arguments, and termination signals, waits for child shutdown, and preserves exit semantics. An end-to-end regression test proves the child receives `SIGTERM`. | Resolved |
| 2026-07-09 | PostgreSQL migration cleanup used throws inside `finally`, which could mask the primary migration or conformance failure. | `packages/meerkat-relay/src/postgres/migrate.ts`, `packages/meerkat-relay/src/postgres/conformance/store-conformance.ts`, relay lint | Captured cleanup failures and surfaced them after cleanup, aggregating scenario and disposer failures when both occur. | Resolved |
| 2026-07-09 | Relay typecheck rejected a future-migration readiness fixture because the migration interface did not satisfy the schema guard's record-shaped query row. | `packages/meerkat-relay/src/postgres/__tests__/postgres-foundation.test.ts`, relay typecheck | Projected checked-in migrations into plain version, name, and checksum ledger records before composing the future-version fixture. | Resolved |
| 2026-07-09 | `node-postgres` allowed `DATABASE_URL` query parameters to override explicit TLS, application-name, and timeout policy. | `packages/meerkat-relay/src/postgres/pool.ts`, Codex uncommitted review | Restricted the managed pool to parameter-free `postgres://` or `postgresql://` URLs and added downgrade regression tests. | Resolved |
| 2026-07-09 | Service roles could inherit stale managed-table privileges granted to PostgreSQL `PUBLIC`. | `packages/meerkat-relay/src/postgres/roles.ts`, Codex uncommitted review | Role reconciliation now revokes schema, table, and sequence privileges from `PUBLIC` across every managed schema before applying exact service grants; live coverage verifies the inherited grant is removed. | Resolved |
| 2026-07-09 | PostgreSQL role reconciliation attempted superuser-only attribute changes while requiring execution as a normal schema owner. | `packages/meerkat-relay/src/postgres/roles.ts`, Codex structured review | Moved role-attribute enforcement to fail-closed validation of infrastructure-provisioned roles, leaving the owner-run transaction limited to grants and revocations. | Resolved |
| 2026-07-09 | The community database role could not remove public-post rows during signed tombstone and GDPR deletion flows. | `packages/meerkat-relay/src/postgres/roles.ts`, Codex structured review | Split public posts from append-only tombstones and granted `DELETE` only on removable `community.public_posts` rows. | Resolved |
| 2026-07-09 | Lockfile-only PostgreSQL driver changes skipped the live database CI job. | `.github/workflows/ci.yml`, Codex structured review | Added `pnpm-lock.yaml` to the relay path filter so PostgreSQL integration coverage runs for driver and transitive dependency changes. | Resolved |
| 2026-07-09 | A delegated grant-operator fixture retained column-level ACL grantor dependencies during cleanup. | `packages/meerkat-relay/src/postgres/__tests__/postgres-integration.test.ts`, `pnpm --filter @mylife/meerkat-relay test:postgres` | Replaced the artificial delegation fixture with a dedicated database owned by a normal non-superuser role, which directly verifies the production ownership model and drops cleanly with the database. | Resolved |
| 2026-07-09 | The migration runner trusted a structurally valid ledger when non-owner table or column ACLs could forge or maintain it, or when an unlogged ledger could be truncated after crash recovery. | `packages/meerkat-relay/src/postgres/migrate.ts`, Codex verification reviews | Ledger validation now requires permanent persistence and rejects every non-owner table-level and column-level write or PostgreSQL 17 `MAINTAIN` ACL. Live coverage exercises an unlogged ledger plus hostile `PUBLIC` insert, checksum-update, and maintenance grants. | Resolved |
| 2026-07-09 | The initial push schema allowed only one capability per registration and stored no registration-secret hash. | `packages/meerkat-relay/src/postgres/migrations/0002-push-archive-ops.ts`, Codex verification review | Added separate registration, capability, and attempt contracts with one-to-many scoped capabilities, rotation generations, secret hashes, revocation, expiry, and retry state. | Resolved |
| 2026-07-09 | The archive schema first collapsed a multi-object manifest, then incorrectly tied content-global object rows to one job and could cascade shared metadata while another publication remained active. | `packages/meerkat-relay/src/postgres/migrations/0002-push-archive-ops.ts`, Codex verification reviews | Added content-global child object rows independent of publication jobs and expanded jobs, scans, and pins for signed rights, leases, and serving state. Live coverage preserves a shared object when one of two referencing jobs is removed. | Resolved |
| 2026-07-09 | A caller could configure the client query timer to fire before PostgreSQL canceled a write, creating an ambiguous committed result. | `packages/meerkat-relay/src/postgres/pool.ts`, Codex verification review | Enforced at least 1000 ms of headroom above the server statement timeout and added boundary coverage. | Resolved |
| 2026-07-09 | Active transactions could retain locks indefinitely by continually issuing short statements. | `packages/meerkat-relay/src/postgres/pool.ts`, Codex verification review | Added a bounded PostgreSQL 17 `transaction_timeout` startup option and enforced that it exceeds the statement timeout. | Resolved |
| 2026-07-09 | Live PostgreSQL tests failed because `aclexplode` rejected a dimensionless empty ACL array used by the new column-grant validator. | `packages/meerkat-relay/src/postgres/migrate.ts`, PostgreSQL 17 integration suite | Replaced the empty fallback with PostgreSQL's one-dimensional column ACL default, then reran all 9 live integration tests successfully. | Resolved |
| 2026-07-10 | Two DMCA claims submitted in the same millisecond could derive the same ID, causing the second immutable insert to collide with the first. | `packages/meerkat-relay/src/dmca-intake.ts`, focused legal-store tests | Added a random per-submission nonce before hashing the claim ID; the 26-test legacy legal pipeline passes with both claims preserved. | Resolved |
| 2026-07-10 | The moderation PostgreSQL integration suite reused the shared parent database and failed when prior migration-runner tests left valid future-version fixtures in its ledger. | `packages/meerkat-relay/src/postgres/stores/__tests__/moderation-stores.integration.test.ts` | Moved moderation races into a unique temporary database created from the guarded test database, then force-dropped it after the suite. | Resolved |
| 2026-07-10 | Archive file-mode contracts accepted arbitrary scan IDs, but PostgreSQL rejected them because `archive.scans.scan_id` is UUID typed. | `packages/meerkat-relay/src/archive-lifecycle.ts`, `PostgresArchiveLifecycleStore` live integration | Tightened the shared scan contract and both adapters to canonical UUIDs, updated conformance fixtures, and reran the live PostgreSQL suite. | Resolved |
| 2026-07-10 | The function gate retained a global no-delete assertion after the persona service gained the narrow tombstone delete privilege required by GDPR replacement writes. | `packages/meerkat-relay/src/postgres/__tests__/postgres-foundation.test.ts`, `pnpm gate:function:changed` | Replaced the stale global regex with role-specific assertions: community remains append-only while persona can delete tombstones only for the GDPR flow. | Resolved |
| 2026-07-10 | A public-directory bind collision emitted an unhandled WebSocket server error after the HTTP listen failure, bypassing startup cleanup and PostgreSQL pool closure. | `packages/meerkat-relay/src/public-directory-node.ts`, first-party bin startup failure test | Unified HTTP and WebSocket startup rejection, cleared heartbeat and sweep timers on failure, and installed handled runtime error listeners so the bin can close its selected state authority. | Resolved |
| 2026-07-10 | The PostgreSQL public-post adapter replaced immutable tombstone sets with DELETE plus reinsert, unnecessarily requiring tombstone deletion privileges in community and persona service roles. | `packages/meerkat-relay/src/postgres/stores/community-stores.ts`, least-privilege review | Changed PostgreSQL tombstone persistence to append-only `ON CONFLICT DO NOTHING` and removed tombstone DELETE from the persona grant. | Resolved |
| 2026-07-10 | Hosted storage keyset pagination repeated an expired manifest when PostgreSQL timestamps differed only below JavaScript's millisecond precision. | `packages/meerkat-relay/src/postgres/stores/hosted-storage-metadata-store.ts`, PostgreSQL 17 hosted storage integration | Projected exact microsecond timestamp strings for reservation, reconciliation, and manifest cursors; the two-pool PostgreSQL 17 suite then passed all 5 tests. | Resolved |
| 2026-07-10 | Hosted storage release accepted the upload observation used for activation, so quota metadata could be released without a confirmed object-store deletion. | `packages/meerkat-relay/src/hosted-storage-metadata.ts`, scoped storage review | Added a typed deletion receipt to the object-store boundary and require its matching object key, version, and confirmed result before releasing activated quota. | Resolved |
| 2026-07-10 | Hosted storage reserve assigned the tenant lifecycle version from a prior read, allowing a concurrent expiry worker to make that version move backward. | `packages/meerkat-relay/src/postgres/stores/hosted-storage-metadata-store.ts`, scoped storage review | Insert reservations conflict-free with an invisible provisional fence, then atomically increment and return the current tenant lifecycle version before publishing the final fence in the same transaction. | Resolved |
| 2026-07-10 | Push invalidation paths twice failed when joined UPDATE statements used ambiguous fencing or lifecycle columns. | `push-stores.ts`, migration 5 legacy-registration trigger, PostgreSQL 17 push integration | Qualified every affected target-table column, retained explicit `legacy_unbound` attempt classification, and reran the migration plus two-pool push suites. | Resolved |
| 2026-07-10 | Push attempt keyset pagination skipped a row when several attempts had different PostgreSQL microseconds inside one JavaScript millisecond. | `packages/meerkat-relay/src/postgres/stores/push-stores.ts`, PostgreSQL 17 push integration | Projected exact six-digit UTC cursor timestamps and passed them back to PostgreSQL without Date truncation; the live v5 migration and push suite then passed all 9 tests. | Resolved |
| 2026-07-10 | Reclaiming a retryable push attempt changed its state to leased without restoring the schema-required pending provider status. | `packages/meerkat-relay/src/push-store-file.ts`, `packages/meerkat-relay/src/postgres/stores/push-stores.ts` | Both adapters now set provider status to pending on every claim; file and PostgreSQL 17 retry tests verify the second fenced claim. | Resolved |
| 2026-07-10 | The PostgreSQL foundation inventory assertion still expected the superseded hosted storage ingest identifier after the production adapter was renamed to storage metadata. | `packages/meerkat-relay/src/postgres/__tests__/postgres-foundation.test.ts`, focused PostgreSQL foundation suite | Updated the expected inventory identifier to `hosted.storage-metadata`; the focused suite passes with the canonical inventory. | Resolved |
| 2026-07-10 | The hosted PostgreSQL role could delete every hosted table, including tenant roots whose foreign keys cascade through quota and object metadata. | `packages/meerkat-relay/src/postgres/roles.ts`, adversarial Phase 1 review | Split hosted grants by adapter behavior: only persona bindings and manifests retain `DELETE`; tenant, policy, reservation, object, subscription, purchase, and link tables cannot be cascade-deleted by the service role. | Resolved |
| 2026-07-10 | Queued or retryable push attempts could still be claimed after their registration, capability, or provider-token generation was revoked or expired. | `packages/meerkat-relay/src/postgres/stores/push-stores.ts`, adversarial Phase 1 review | Revocation and invalidation now cancel all nonterminal matching attempts and invalidate active leases; claim also revalidates the full capability, registration, provider, and token graph using database time before returning work. | Resolved |
| 2026-07-10 | Releasing an operations lease or abandoning an idempotency claim deleted its row, allowing the next claim to reuse fencing token 1 and a stable owner to make stale work look current. | `packages/meerkat-relay/src/postgres/stores/operations-store.ts`, adversarial Phase 1 review | Release and abandon now expire but preserve their durable row; every reacquisition increments the existing fence, and two-pool regressions reject the old token even for the same owner. | Resolved |
| 2026-07-10 | Humanity and persona HTTP boundaries translated PostgreSQL authority outages into generic HTTP 500 responses. | `packages/meerkat-relay/src/humanity-service-http.ts`, `packages/meerkat-relay/src/persona-service-http.ts`, adversarial Phase 1 review | Both boundaries now preserve the typed unavailable signal as HTTP 503 `state_authority_unavailable`; focused health-route regressions cover both services. | Resolved |
| 2026-07-10 | Persona and community services could send one-time humanity tokens through inconsistent clients, including a plaintext path with no deadline or response-size bound. | `humanity-redeem-client.ts`, persona and community bins, adversarial Phase 1 review | Both bins now share strict URL validation, HTTPS by default, explicit trusted-network HTTP opt-in, a 5-second abortable deadline, and a 16 KiB response ceiling; compose opts into its internal service network explicitly. | Resolved |
| 2026-07-10 | A reserved persona registration had no expiry or recovery sweep, so an abandoned or unknown humanity request could hold an alias and persona key forever. | `packages/meerkat-relay/src/persona-registry.ts`, file/PostgreSQL persona stores, migration 4, adversarial Phase 1 review | Added a bounded 24-hour reservation lease, exact-attempt reclaim after expiry, bounded PostgreSQL cleanup, and sweep recovery that commits durable `humanity_verified` attempts after a process crash. | Resolved |
| 2026-07-10 | The package root omitted the PostgreSQL operator console, NCMEC queue, and DMCA adapters, blocking runtime consumers that import from `@mylife/meerkat-relay`. | `packages/meerkat-relay/src/index.ts`, adversarial Phase 1 review | Added all three moderation adapters to the public package barrel while retaining their PostgreSQL sub-barrel exports. | Resolved |
| 2026-07-10 | NCMEC evidence accepted unbounded hash arrays and overall JSON payloads, allowing oversized legal-queue rows and exports. | `packages/meerkat-relay/src/ncmec-queue.ts`, file/PostgreSQL NCMEC stores, migration 3, adversarial Phase 1 review | Added shared validation with a 256-hash and 64 KiB ceiling across memory, file, and PostgreSQL boundaries plus a database payload constraint. | Resolved |
| 2026-07-10 | Legacy hosted tenants lacked policy rows, while legacy objects with null versions, zero bytes, or non-SHA checksums were unreadable by the new metadata adapter. | migration 3, hosted storage metadata contract/store, adversarial Phase 1 review | Backfilled conservative tenant policies, classified compatible objects as verified, quarantined incompatible metadata as `legacy_unbound`, excluded it from serving paths, and exposed exact readiness counts for operator reconciliation. | Resolved |
| 2026-07-10 | A restarted public-directory replica reported zero live host RIDs because PostgreSQL observability used a process-local counter. | `PostgresDirectoryHostAnnouncementStore.stats`, public directory `/healthz`, adversarial Phase 1 review | Made host statistics authoritative and asynchronous, count distinct unexpired RIDs using PostgreSQL time, await that result in node health, and added a fresh-replica integration assertion. | Resolved |
| 2026-07-10 | PostgreSQL rejected the archive v6 migration because a CHECK constraint used the unsupported `{1,512}` regular-expression repetition bound. | `0006-archive-lifecycle.ts`, focused PostgreSQL 17 archive migration test | Replaced the oversized quantifier with an explicit length check plus an unbounded allowed-character regex; the migration retains the same validation without exceeding PostgreSQL's regex bound. | Resolved |
| 2026-07-10 | Community node private feed state silently ran on the in-memory default in every mode because the bin never passed `privateStateStore` to `CommunityNode`, so challenges, rate windows, publish stages, and sealed tail did not survive restarts even for file self-hosters. | `bin/meerkat-community-node.mjs`, Plan 44 Phase 1 WP-B | Wired `FileCommunityPrivateStateStore` in file mode and `PostgresCommunityPrivateStateStore` in first-party mode in commit `7b8fa151`; see [session log](docs/sessions/2026-07-10-meerkat-plan44-phase1-complete.md). | Resolved |
| 2026-07-10 | Fail-closed community bin spawns littered the package root with `.meerkat-community/post-receipt.seed` because the test helper cleared `DATA_DIR` without a replacement and the bin persists its post-receipt seed before the state authority resolves. | `src/postgres/__tests__/community-service-bin-state-authority.test.ts`, Phase 1 adversarial review | Helper now defaults `DATA_DIR` to a per-process temp path; recreation verified impossible in commit `7b8fa151`. | Resolved |
| 2026-07-10 | WP-2A file object store stored bytes base64 inside the JSON metadata ledger: O(entire store) rewrite per mutation, whole-store memory residency, JSON.parse fails near the V8 string cap. | `packages/meerkat-relay/src/object-store-file.ts`, Phase 2 adversarial review | Rejected at review; reworked to bytes-as-individual-files with a metadata-only ledger, write-ahead ordering, and streaming multipart assembly in commit `2710d33a`. | Resolved |
| 2026-07-10 | Hosted ingest cap accounting paged the ENTIRE bucket per 4 MiB block because listInventory had no prefix scoping; O(bucket) S3 LIST calls under the tenant write lock. | `packages/meerkat-relay/src/storage-ingest-store-object.ts`, Phase 2 adversarial review | Rejected at review; additive prefix amendment to the object-store contract across all three adapters plus a first-use prefix sweep and write-lock counter in commit `1d4238aa`. | Resolved |
| 2026-07-10 | Pre-commit staged-snapshot gate failed twice for a partial WP-2C commit: the hook stashes unstaged work and typechecks the staged snapshot alone, which was inconsistent while two agents' barrel edits interleaved in src/index.ts. | `.husky/pre-commit`, WP-2C/WP-2D commit attempts | Committed the two reviewed packages as one full-snapshot unit (`1d4238aa`), which passed the same gate; safety stash preserved by the hook was verified redundant. | Resolved |
| 2026-07-10 | Archive promote dual-write ordering was inverted (promote before reference-edge write), leaving a crash window whose durable-orphan residue the reconciler would eventually delete: silent archive data loss. | `packages/meerkat-relay/src/archive-object-bytes.ts`, Phase 2 WP-2E adversarial review | Rejected twice at review (agent message races); edge-first ordering with promote-failure edge cleanup and a crash-window proof against the real reconciler landed in commit `c5e81b09`. | Resolved |
| 2026-07-10 | Shadow comparator's unclassified-method guard threw AFTER the primary call executed, so a misconfigured deployment would apply a side effect and then error the caller, inviting a duplicate-effect retry. | `packages/meerkat-relay/src/shadow-state-comparator.ts`, Phase 3 WP-3B adversarial review | Guard moved before the primary invocation at review; suites re-verified; committed in `9bbb8362`. | Resolved |
| 2026-07-10 | Commit `957c282c` interleaved the rewritten migration 10 schema with older cutover store/CLI code addressing dropped columns; the pre-commit hook passed it because live-gated suites skip without database env vars. | `packages/meerkat-relay` cutover files, WP-3C mid-rewrite snapshot | Agent flagged the interleave; reconciliation committed as `ba46ab89` after a lead-run live battery. Process lesson: the staged gate proves nothing about live-suite coherence; run the live battery before committing schema-plus-consumer changes. | Resolved |
| 2026-07-10 | Restore-smoke could digest the live ops database as the "restore", replay a proof id and exit 0 against a durable verified=false row, and land stale evidence after lease expiry; freshness kept a future-dated proof "fresh" and a malformed SLO file softened to permissive defaults. | `packages/meerkat-relay/src/postgres/{backup-cli,backup-store,backup-evidence}`, `synthetics/backup-freshness.mjs`, Phase 5 WP-5A Codex adversarial review | Seven P1 + two P2 findings fixed in `b792a124`: same-target guard, releaseSha/timestamp binding, durable-row duplicate semantics, lease-fenced insert, future-proof and SLO hard-fails, recomputed proof digests, redacted fatals. | Resolved |
| 2026-07-10 | Backup snapshot recorded --digest-role as an unverified operator claim; a writer-credential connection would carry a false audit trail in the evidence artifact. | `packages/meerkat-relay/src/postgres/cli/backup-cli.ts`, Phase 5 WP-5A lead review | Snapshot now records the connection's actual `current_user` via `resolveDigestRole`; an asserted --digest-role that mismatches fails fast. In `feb943e5`. | Resolved |
| 2026-07-11 | Release gate honesty holes: --verify trusted a tamperable stored manifest (comment claimed otherwise), duplicate --record exited 0 while a DIFFERENT manifest stayed durable, the exceptions expiry audit was bypassed by any line containing EXAMPLE, only-fixed scan filtering contradicted the stated guarantee, unscoped CVE ignores allowed, duplicate image pins allowed, write permissions leaked to non-build jobs. | .github/workflows/release-images.yml, packages/meerkat-relay/src/release + cli/release-cli.ts, Phase 6 WP-6A/6B Codex adversarial review | 8 P1 + 3 P2 findings fixed pre-commit: canonical-digest tamper check, duplicate idempotent/conflict exit semantics, structural YAML exceptions audit (scope + per-entry expiry), informational full scan artifact, job-scoped permissions, unique-image schema refinement, stated gate boundaries. In WP-6A/6B commits. | Resolved |
| 2026-07-11 | Promotion ladder honesty holes: latest-state selection ambiguous on timestamp ties, promotion insert not lease-fenced (stale forward row after rollback possible), production recordable with no canary evidence, NDJSON evidence truncated to last line, rollback allowed to a rolled-back or unprovably-ordered target, reversal claims smuggleable at depth, canary aggregate read unknown verdicts as green, generated ids collidable, workflow inputs renderable into forged checklists, cosign guidance used a wildcard identity. | packages/meerkat-relay promotion store/CLI/migration 11, canary-verdict.mjs, deploy-staging/promote-canary/rollback-release workflows, Phase 6 WP-6C Codex adversarial review + lead review | 11 of 13 findings fixed pre-commit in the WP-6C commit (seq total order, lease-fenced insert, hard evidence gate, full NDJSON attachment, target-state and fail-closed ordering checks, deep reversal scan, fail-closed aggregation, random id suffix, input-format guards, pinned cosign identity); DB-level continuity documented as a stated trust boundary. | Resolved |
| 2026-07-11 | roles.function-gate memory-budget test failed 2 of 3 isolated runs; the Phase 5 close-out had misdiagnosed it as a transient GC flake. Root cause: assertMemoryBudget never forced GC without --expose-gc, so the heap delta measured collection timing, not the function. | packages/meerkat-relay/src/test/function-quality.ts, pnpm test | Runtime GC handle acquired via v8 setFlagsFromString + scratch-context gc, with a min-of-five-attempts fallback that still catches real leaks; five consecutive isolated runs green. Fixed in the test-infra commit preceding WP-6C. | Resolved |
| 2026-07-11 | Live read paths in backup-cli, promotion-cli, and release-cli fataled: the bracket application-name convention ([meerkat_ops]) is rejected by the pool validator; existing tests never reach openPool with a live connection so the defect shipped in Phases 5 and 6. | packages/meerkat-relay/src/postgres/cli/{backup,promotion,release}-cli.ts | Found by the WP-7A agent, reproduced live by the lead, all seven names switched to validator-safe ':' separators, all three CLIs verified answering live. Fixed in the app-name commit before WP-7A. | Resolved |
| 2026-07-11 | Rehearsal passed-requires-evidence invariant was fully bypassable with vacuous evidence ({a:null}, nested empties, empty attachedEvidence wrappers) recording passed drills with exit 0, contradicting the migration's own honesty claim; plus loose started_at parsing silently reinterpreted claimed instants and the export dropped the stamped releaseApproved flag. | packages/meerkat-relay rehearsal store/CLI/migration 12, WP-7A adversarial review (Opus, live-confirmed; Codex rate-limited) | Deep-vacuity gate at CLI + store + jsonb_strip_nulls DB backstop, strict ISO started_at with project floor and split refusal reasons, releaseApproved surfaced in the export; regression tests for every vector. Fixed in the WP-7A commit. | Resolved |
| 2026-07-11 | Load/soak harness false-green vectors: load-http counted application errors as completed too (error rate diluted below its ceiling; a 1%-shape-leaking /healthz passed a 1% gate), and a soak under window*2 samples skipped all memory-growth checks (an 800% RSS ramp passed as held_steady); three drill runbooks invoked the harness with flags it does not parse, self-blocking the drills. | packages/meerkat-relay/scripts/{load,soak}, docs/guides/meerkat-runbooks/{failover,secret-rotation,queue-backlog} drills, Phase 7 WP-7B/7C adversarial review (Opus, live-confirmed; Codex rate-limited) | Mutually exclusive completed/errors accounting (live-verified fail on an all-error run), insufficient_samples breach with regression test, real --target/--load-relay command forms with the prefixed metric-growth wiring. Fixed pre-commit in the WP-7B/7C commits. One reviewer claim (metricSnapshot prefix mismatch) was refuted: snapshot and aggregate use the identical lookup. | Resolved |
| 2026-07-11 | Phase 7 load/soak harness has two narrow fail-open verdict paths: (1) a p99 in the histogram overflow region reports the top-bucket floor (262144ms), which passes a latency budget set above that bound; (2) evaluateLoadRun does not assert completed+errors==attempted, so abandoned-uncounted attempts (attempted 10000, completed 1, errors 0) verdict `ok`. Found by a Codex adversarial review; its error-rate probes were degenerate-input false positives (verified: the error-rate gates fail correctly with realistic inputs). | `packages/meerkat-relay/scripts/load/lib/stats.mjs`, `scripts/load/lib/verdict.mjs`, Phase 7 Codex review | Fixed in `337405a4`: summarizeLatency now surfaces per-percentile floor flags and evaluateLoadRun breaches an overflow-floor percentile against any budget; evaluateLoadRun also fails an abandoned-operations gap above a 2% ceiling. Verified both exploits close and legitimate passes survive. | Resolved |
| 2026-07-12 | Fresh mynews-plan48 worktree: pnpm install exited 0 but left modules/mynews/node_modules unlinked (zod unresolvable, vitest suite hard-failed); a second interactive-prompted install wanted to purge and rebuild | .claude/worktrees/mynews-plan48, pnpm install | CI=true pnpm install --prefer-offline relinked all packages non-interactively; suites green afterward. Worktree installs should always run with CI=true | Resolved |
| 2026-07-11 | WP7 module verification initially failed two tests: boolean capability parsing and an incorrect expected earnings balance. | `cd modules/mynews && npx vitest run --reporter=dot` | Handle boolean switches before string parsing and correct the ledger arithmetic assertion; rerun passed 671/671 tests. | Resolved |
| 2026-07-11 | WP7 app tests could not parse React Native's Flow entrypoint because a pure capability helper imported `Platform`. | `cd apps/mynews && npx vitest run --reporter=dot` | Keep React Native out of the data helper and derive an injectable platform from `EXPO_OS`; rerun passed 190/190 tests. | Resolved |
| 2026-07-11 | WP7 app typecheck could not resolve the newly declared native RevenueCat package before the final install and found a widened billing product ID. | `cd apps/mynews && npx tsc --noEmit` | Structurally type the native SDK boundary, keep native imports out of the node graph, and narrow the configured product to `ProductId`; rerun passed. | Resolved |
| 2026-07-11 | WP7 privacy narrowing temporarily removed the supporter ID from receipt mapping instead of only the journalist ledger query; diff check also found three blank EOF lines. | `apps/mynews/app/(root)/data/support-client.ts`, `git diff --check`, app typecheck | Restore the receipt self-ID mapping, redact it only from journalist earnings, remove EOF whitespace; typecheck and diff check pass. | Resolved |
| 2026-07-14 | Sync typecheck rejected the narrowed Box folder-id assignment during WP-41E implementation. | `packages/sync/src/storage/adapters/box.ts`, `pnpm --filter @mylife/sync typecheck` | Declared the mutable folder id explicitly as `string | null`; sync typecheck is green. | Resolved |
| 2026-07-14 | Sync typecheck rejected an inferred `reduce` accumulator in the Box fake-provider commit assembly. | `packages/sync/src/storage/__tests__/adapters/box-conformance.test.ts`, `pnpm --filter @mylife/sync typecheck` | Replaced the ambiguous typed-array reduction with an explicitly typed iterative concatenation; sync typecheck is green. | Resolved |
| 2026-07-14 | The sync function gate found an unused import and four unnecessary string escapes in the OneDrive fake-provider suite. | `packages/sync/src/storage/__tests__/adapters/onedrive-conformance.test.ts`, `pnpm gate:function` | Removed the unused import and simplified the quoted eTag/cTag literals; the 67-case adapter gate is green. | Resolved |
| 2026-07-14 | Source audit found that real OneDrive and Box reads would reject their documented preauthenticated content locations, and Box direct upload encoded its SHA-1 header in the wrong format. | `packages/sync/src/storage/adapters/{onedrive,box}.ts`, WP-41E provider protocol audit | Added host-allowlisted, unauthenticated capability fetches with unknown redirects still refused; changed Box `Content-MD5` to hexadecimal SHA-1 and enforced both paths in fake transports. | Resolved |
| 2026-07-14 | Upload-session creation races skipped the required byte comparison, and a lost Box commit response could turn an accepted upload into `not_found`. | `packages/sync/src/storage/adapters/{onedrive,box}.ts`, WP-41E resumability audit | Race conflicts now re-read and verify ciphertext; Box returns a total-offset resume token after commit transport failure and recovers a completed file when the session disappears. | Resolved |
| 2026-07-14 | Web typecheck rejected a browser-only destination-kind union at the broker adapter seam and an invalid restore-state report field. | `apps/meerkat-web/src/ui/settings/StorageOverlay.tsx`, `pnpm --filter meerkat-web typecheck` | Narrowed the broker destination kind before the async closure and read the failed restore report from its activation result; web typecheck is green. | Resolved |
| 2026-07-14 | WP-41H integration typechecks rejected an optional broker credential ref, diagnostics field names, and an underspecified web snapshot database type. | `packages/sync/src/storage/lifecycle.ts`, mobile backup scheduling, web scheduling runner | Narrowed the credential before constructing broker authorization, used diagnostic camel-case fields, and required the web snapshot export/flush interface; all three typechecks are green. | Resolved |
| 2026-07-14 | Sync typecheck rejected function-gate tests because the scaffold generated a helper import outside the package root. | `packages/sync/src/storage/__tests__/*.function-gate.test.ts`, `pnpm --filter @mylife/sync typecheck` | Pointed all six tests at the existing package-local `src/test/function-quality.ts` helper; focused tests and sync typecheck pass. | Resolved |
| 2026-07-14 | The scheduler function-gate memory probe exceeded 8 MiB while constructing twenty fresh 500-policy fixtures without exposed GC. | `storage-scheduler.function-gate.test.ts`, `pnpm gate:function --package @mylife/sync` | Kept the 8 MiB ceiling and calibrated the repeated allocation probe to ten 250-policy batches; the separate 1,000-policy complexity check remains intact and the full function gate passes. | Resolved |
| 2026-07-14 | Sync typecheck rejected generic typed-array envelopes and an invalid object-chunk data-class lookup in the WP-41I adversarial fixture. | `packages/sync/src/storage/__tests__/storage-adversarial-e2e.test.ts`, `pnpm --filter @mylife/sync typecheck` | Copied codec envelopes into ArrayBuffer-backed arrays and used the vector object's known attachment class; focused tests and sync typecheck pass. | Resolved |
| 2026-07-15 | Plan 41 completion commits are absent from current `main`, `origin/main`, and `feature/meerkat-plan43`. | Plan 41 integration topology; completion audit H1 | Remediation `f29ab80d` was merged with the complete Plan 43 tip in `6b70b994`; migration collisions were renumbered 13 through 16 and all integrated gates pass. Promotion to the protected release branch remains a separate workflow step. [Audit](docs/reports/REPORT-meerkat-plan41-completion-audit-2026-07-15.html). | Resolved |
| 2026-07-15 | Most mobile storage connect flows are placeholders; hosted and connected destination factories return null on both clients. | Mobile StorageProvider/add-destination, mobile/web destination registries; completion audit H2 | Real native PKCE/SecureStore flows and configured mobile/web registries now cover every destination, including hosted and connected storage; foreground/background registry parity and product-flow tests pass in `f29ab80d`. | Resolved |
| 2026-07-15 | Production storage backups include SQLite chunks but omit recovery identity and attachment/library object bytes. | Mobile `local-backup-run.ts`, web `web-backup-run.ts`; completion audit H3 | Production runners now stream recovery identity, database, and every blob including zero-byte objects; real runner-to-staged-restore tests and missing identity/blob fail-closed tests pass in `f29ab80d`. | Resolved |
| 2026-07-15 | Production backup orchestration retains every encrypted chunk until the job and mirror finish, making memory O(total backup size). | Mobile/web backup runners and sync storage router; completion audit H4 | Filesystem mobile and Blob/IndexedDB web payload stores keep only descriptors in the router and load one staged payload at a time; buffers zero in `finally`; 256 MiB adversarial and function gates pass in `f29ab80d`. | Resolved |
| 2026-07-15 | Persisted storage jobs cannot resume after router or process reconstruction because executable context exists only in memory. | `packages/sync/src/storage/router.ts`; completion audit H5 | Cursor v2 persists rehydratable metadata and staged references; a test destroys the first router, reconstructs a fresh router, and resumes to verified completion in `f29ab80d`. | Resolved |
| 2026-07-15 | Failed web restore activation rolls back durable bytes but leaves the live BrowserDatabaseAdapter closed. | Web `local-restore.ts` and `StorageOverlay.tsx`; completion audit H6 | Post-close failures report `activeDatabaseClosed`; the UI forces controlled reload after rollback, and live-handle fault injection passes in `f29ab80d`. | Resolved |
| 2026-07-15 | Browser WebDAV/S3 secrets persist in a same-origin IndexedDB vault instead of the binding credential-broker boundary. | Web `credential-store.ts` and `browser-secret-store.ts`; completion audit H7 | KMS broker custody, one-way local-vault migration with rollback/revoke, per-operation retrieval, subject/AAD binding, rate limit, tokenless audit, and deletion tests landed in `f29ab80d`; browser persistence now contains only broker references. | Resolved |
| 2026-07-12 | FAIL  src/postgres/__tests__/hosted-service-bin-state-authority.test.ts > hosted service state authority bin > boots and cleanly stops the complete self-hosted… | `pnpm --filter @mylife/meerkat-relay test 2>&1 \| tail -50` | Reverified on the integrated 2026-07-15 tree: the two process-boundary files pass 13 tests in isolation and the complete relay suite passes 185 files / 1,386 tests when run at normal package concurrency. | Resolved |
| 2026-07-12 | WP-43A opus review HIGH: NCMEC evidence silently lost if the scanner crashed between the terminal reject commit and the evidence enqueue (rejected jobs are never re-claimed); plus stranded-scanning jobs never reclaimed and an unbounded per-object scan read | packages/meerkat-relay/src/archive-scanner-worker.ts | Enqueue-before-commit with tuple-derived idempotent evidence id, expired-lease scanning reclaim, 128 MiB scan-memory cap (terminal flagged), mismatch result code split; all test-pinned in e9c1f68e | Resolved |
| 2026-07-12 | WP-43B opus review MEDIUM: archive-seeder bin stubbed cursor persistence to no-ops, so reconciliation restarted from page one every tick (hosts beyond MEERKAT_ARCHIVE_RECONCILE_MAX never fully reconciled) | packages/meerkat-relay/bin/meerkat-archive-seeder.mjs | Real durable cursor via migration 16 ops.archive_pin_reconcile_runs + PostgresPinReconcileCursorStore, wired in the bin; grant + live-PG shared-byte tests added in 4d34905c | Resolved |
| 2026-07-12 | WP-43D opus review MEDIUM: a THROWING injected resolver/puller/committer rejected the whole automatic-history pass instead of degrading, so one network error hid the manual-import fallback and skipped remaining candidates | packages/sync/src/node/community-history-sync.ts | Per-candidate try/catch (throw = failed candidate), resolve_failed fallback reason, cache eviction on throwing cached pull; throwing-wiring tests added in e3a73774 | Resolved |
| 2026-07-12 | WP-43G mid-development failures (auto-logged): TS2300 duplicate 'StoredPrivateCommunityState', meerkat-relay typecheck fail, announce e2e FAIL | wp43g worktree, packages/meerkat-relay | Fixed during WP-43G development; re-verified 2026-07-12: worktree typecheck clean, announce + announce-e2e 15/15 green | Resolved |
| 2026-07-12 | WP-43E mid-development failures (auto-logged): TS2739 incomplete scanner stub in archive-intake-http.test.ts, archive-intake e2e FAIL, archive-lifecycle-store-conformance FAIL | packages/meerkat-relay (main repo) | Fixed during WP-43E development; re-verified 2026-07-12 before commit: package typecheck clean, archive-intake 26/26 + lifecycle conformance 18/18 green | Resolved |
| 2026-07-15 | Relay and web typechecks failed after the Plan 41 and Plan 43 merge because `PostgresObjectDeletionJobStore` was exported twice from the relay barrel. | `packages/meerkat-relay/src/index.ts`; `pnpm --filter @mylife/meerkat-relay typecheck`; `pnpm --filter meerkat-web typecheck` | Removed the redundant direct export and retained the canonical PostgreSQL barrel export; both typechecks pass. | Resolved |
| 2026-07-15 | The production Compose/community-node topology lags the integrated Plan 41/43 code: no OAuth KMS/provider or signed-storage descriptor mounts, no DMCA agent environment, no archive scanner/seeder/NCMEC workers, and WP-43E `archiveIntake` is not composed by the production bin. | `packages/meerkat-relay/deploy/compose.production.yml`, `bin/meerkat-community-node.mjs`, `bin/meerkat-hosted-service.mjs`; full production-readiness refresh | Composed signed storage and OAuth secret mounts, archive intake, ClamD scanner, seeder, NCMEC filer, DMCA fields, least-privilege roles, readiness gates, and production topology tests. | Resolved |
| 2026-07-15 | Mobile, web, and native-call typechecks could not resolve the newly merged call-native and LiveKit packages. | Plan 25 integration; affected package typechecks | Ran `pnpm install` against the merged lockfile, which linked the new workspace and LiveKit dependencies; all five affected typechecks then passed. | Resolved |
| 2026-07-15 | Plan 25 integration pre-commit failed because the bad-signature test sometimes replaced a trailing `0` with the same value, leaving a valid signature. | `packages/sync/src/protocol/__tests__/call-session.test.ts`; `pnpm gate:function:changed --staged` | Mutate the final hex character deterministically to the opposite value; the 45-test call-session suite and clean sync lint pass. | Resolved |
| 2026-07-15 | DMCA startup validation treated the canonical `first-party` deployment profile as self-hosted, so production could boot without registered-agent data. | `packages/meerkat-relay/src/dmca-config.ts`; production Compose | Accept both the canonical hyphenated profile and the legacy underscore spelling; added a fail-closed regression test. | Resolved |
| 2026-07-15 | Relay typecheck rejected the new NCMEC HTTP-client fixture because it omitted required durable queue fields. | `packages/meerkat-relay/src/__tests__/ncmec-filing-http-client.test.ts`; relay typecheck | Made the fixture a complete `queued` `NcmecReportRecord` and pinned it with `satisfies`. | Resolved |
| 2026-07-15 | Meerkat mobile and web typechecks rejected publish-view fixtures after authoritative managed archive status became required. | Mobile and web `public-publish.test.ts`; package typechecks | Added the real lifecycle status represented by each pending, approved, and rejected fixture. | Resolved |
| 2026-07-15 | Relay typecheck could not resolve the DOM-only `BodyInit` name imported transitively from the React Native managed archive client. | `packages/sync/src/node/managed-archive-client.ts`; relay typecheck | Kept the runtime `Uint8Array` upload body and used a local type-only cast that does not require DOM library declarations. | Resolved |
| 2026-07-15 | Web typecheck rejected the new archive-status test helper name and an unexported HLC comparator in the manual history fallback. | Web public-publish tests and `channel-history-import.ts`; web typecheck | Used explicitly typed fetch fixtures and the local HLC wall/counter ordering used by the existing web data layer. | Resolved |
| 2026-07-15 | Web managed-archive status test failed before exercising status because its fresh in-memory database lacked the Meerkat tables. | Web `public-publish.test.ts`; targeted Vitest run | Applied the same `ensureSyncSchema` fixture setup used by the adjacent archive persistence test. | Resolved |
| 2026-07-15 | Automatic history sync counted and replicated events inside a transaction callback, so a rolled-back commit followed by host fallback could report or replicate uncommitted events. | Mobile and web `syncAutomaticCommunityHistory`; production-readiness review | Accumulate merge results transaction-locally and publish the counts and replication records only after the transaction returns successfully. | Resolved |
| 2026-07-15 | Managed archive status and cancel responses were trusted via TypeScript casts, allowing malformed remote state to be persisted as authoritative lifecycle status. | `packages/sync/src/node/managed-archive-client.ts`; production-readiness review | Added bounded JSON decoding plus exact job-id, status, byte-count, scan-outcome, error-code, and state-coherence validation with negative tests. | Resolved |
| 2026-07-15 | A long-running ClamD worker verified engine and signature provenance only at startup, so a later FreshClam reload could make durable scan provenance stale. | `packages/meerkat-relay/src/archive-malware-clamd.ts`; production-readiness review | Bracket every INSTREAM scan with exact VERSION checks; a signature change during the scan now fails closed and is regression-tested. | Resolved |
| 2026-07-15 | The NCMEC gateway client passed an arbitrary provider error string into the bounded durable filing queue. | `packages/meerkat-relay/src/ncmec-filing-http-client.ts`; production-readiness review | Accept only stable bounded error-code tokens and normalize all other provider text to `provider_rejected`; added a regression test. | Resolved |
| 2026-07-15 | Managed archive quota used a read-then-enqueue check, so concurrent creates for one owner could both pass and overbook the tenant cap. | Archive intake and lifecycle stores; production-readiness review | Moved quota reservation into lifecycle enqueue; file mode holds its exclusive lock and PostgreSQL holds an owner-scoped transaction advisory lock across sum and insert. Added memory, file, and live-PostgreSQL concurrency tests. | Resolved |
| 2026-07-15 | The new owner-quota conformance test used Vitest `.resolves` directly on the synchronous in-memory store result. | `archive-lifecycle-store-conformance.test.ts`; targeted relay test | Wrapped the store result with `Promise.resolve` so the same assertion covers synchronous memory and asynchronous file/PostgreSQL adapters. | Resolved |
| 2026-07-15 | Managed publishing acquired an entitlement for archive intake but omitted it from the hosted publication registration request, so first-party nodes rejected every managed publish before upload. | Mobile and web `public-publish.ts`; production-readiness review | Send the same short-lived bearer to hosted register and archive-intake requests, reject missing managed configuration before building, and cover successful and failed upload sequences in both app suites. | Resolved |
| 2026-07-15 | The web managed-publish tests used the mobile suite's `fetchMock` helper, which does not exist in the web twin. | Web `public-publish.test.ts`; targeted Vitest run | Replaced it with explicitly typed `vi.fn` fetch fixtures used by the web test environment. | Resolved |
| 2026-07-15 | The new mobile automatic-history composition test omitted the app-owned `mk_settings` schema. | Mobile `automatic-community-history.test.ts`; targeted Vitest run | Added `ensureMeerkatTables` to the fixture before installing and syncing the community. | Resolved |
| 2026-07-15 | The web automatic-history composition twin also omitted the separately initialized `mk_settings` schema. | Web `automatic-community-history.test.ts`; targeted Vitest run | Added the web `ensureMeerkatTables` initializer alongside `ensureSyncSchema`. | Resolved |
| 2026-07-15 | The web manual history fallback accepted plaintext HTTP seed URLs and had no direct composition tests. | `channel-history-import.ts`; production-readiness review | Restricted manual seeds to HTTPS and added malformed/catalog, seed filtering, verified merge, cursor, and transaction rollback tests. | Resolved |
| 2026-07-15 | Sync typecheck failed after making the managed archive object manifest signature-bound because existing test fixtures still built legacy manifest-less jobs and used the old submit signature. | `packages/sync/src/__tests__/public-archive.test.ts`, `packages/sync/src/node/__tests__/managed-archive-client.test.ts`; `pnpm --filter @mylife/sync typecheck` | Updated fixtures and client assertions to provide exact signed object manifests and owner request proofs; sync typecheck and the full 2,372-test suite pass. | Resolved |
| 2026-07-15 | Managed archive intake regression tests initially expected the pre-manifest hash error and pre-built two same-content publications before the authoritative resolver lookup, causing two test failures. | `packages/meerkat-relay/src/__tests__/archive-intake-http.test.ts`; targeted Vitest run | Aligned the expected fail-closed code with the signed-manifest boundary and built the second same-content publication only after the first reached pinned state; the 33-test archive proof and full 1,508-test relay suite pass. | Resolved |
| 2026-07-15 | The full sync suite emitted `PromiseRejectionHandledWarning` even though all tests passed because a fake-timer timeout assertion observed the expected rejection only after advancing time. | `packages/sync/src/__tests__/webrtc-transport.test.ts`; `NODE_OPTIONS=--trace-warnings pnpm --filter @mylife/sync test` | Attached the rejection assertion before advancing fake time and removed the global unhandled-rejection suppression; the focused test and warning-traced full suite pass cleanly. | Resolved |
| 2026-07-15 | Two Meerkat web launch-path tests failed because the Playwright server did not provide a hosted API origin, so production code failed closed before the tests' entitlement route intercept could run. | `apps/meerkat-web/playwright.config.ts`, `e2e/launch-paths.spec.ts`; browser QA | Injected a loopback hosted API origin into the Playwright web server so positive and forged-cache tests exercise the real revalidation boundary; all six launch-path tests pass. | Resolved |
| 2026-07-15 | Opening Storage & Backup under the real browser launch harness crashed the React tree because its loopback test API origin was rejected by the credential broker before the overlay could render. | `apps/meerkat-web/src/lib/storage/{credential-store,configured-registry,use-web-storage}.ts`, `ui/settings/StorageOverlay.tsx`; Playwright trace | Preserved HTTPS-only production behavior while explicitly allowing loopback HTTP only in Vite development mode at every broker construction site; added a launch-path regression for the honest empty state. | Resolved |
| 2026-07-15 | The generated production-readiness HTML requested a missing favicon during browser QA. | `docs/reports/REPORT-meerkat-full-production-readiness-2026-07-15.html`, production activation runbook HTML | Embedded a data-URI favicon in both self-contained artifacts; repeat browser checks report zero errors and warnings. | Resolved |
| 2026-07-17 | apps/meerkat typecheck failed post-merge: expo-auth-session and @mylife/meerkat-call-native unresolvable, cascading unknown-type errors in icloud-adapter | integration merge of fix/meerkat-plan41-audit-findings; pre-commit function gate | Merge introduced new workspace deps without an install; `CI=true pnpm install --no-frozen-lockfile` relinked, lockfile committed with the merge; typecheck green | Resolved |
| 2026-07-17 | dowork cloud-coaching offline-queue tests failed on main (2/559): fixtures hardcoded createdAt 2026-07-03, which crossed the DL-4 14-day age backstop on 07-17, so retained items were correctly expired-dropped | apps/dowork/app/(root)/data/__tests__/cloud-coaching.test.ts, full-suite run during the 2026-07-17 integration sweep | Queue fixtures now use a relative RECENT_ISO timestamp; 66/66 file + 559/559 dowork suite green | Resolved |
| 2026-07-17 | meerkat-icloud-storage native-source test failed on merged main: authored ios/MeerkatICloudStorageModule.swift was never tracked (broad `ios/` gitignore rule, no un-ignore exception like native-transport's), so the branch's own gate only passed where the files sat untracked on disk | packages/meerkat-icloud-storage, pre-commit function gate | Recovered both iOS sources from the plan41 worktree, added un-ignore exceptions for meerkat-icloud-storage and meerkat-call-native, tracked in the merge commit; 11/11 package tests green | Resolved |
| 2026-07-13 | Plan 25 calls mid-development failures (auto-logged): ESLint warnings, call-signal-transport vitest FAIL, push-store getRegistration FAIL | meerkat-plan25-calls worktree, packages/sync, packages/meerkat-relay | Fixed during Plan 25/40 closure work merged in b0aaf0dd; re-verified 2026-07-16 on fix/meerkat-plan41-audit-findings (sync 2,372 + relay 1,508 green per readiness report; getRegistration by-hash getter verified in code) | Resolved |
| 2026-07-14 | Plan 41 storage mid-development failures (auto-logged): log-hygiene-canary-e2e FAIL, storage-ui-core pnpm FAIL, restore.tsx TS2739, ESLint warnings | Apps-wt-41-meerkat-storage worktree, apps/meerkat, packages/meerkat-relay | Fixed during Plan 41 remediation (234d7492, 25c38e51); re-verified 2026-07-16: plan43 proof 182/182 + entitlements 77/77 rerun green, app/web suites green per readiness report | Resolved |
| 2026-07-16 | Readiness report claimed archive intake validates SHA-512 object values; code uses SHA-256 (SHA-512 only derives content-registry ids) | docs/reports/REPORT-meerkat-full-production-readiness-2026-07-15.md, packages/meerkat-relay/src/archive-intake-http.ts | Corrected report md + html twin in 118346ae on fix/meerkat-plan41-audit-findings | Resolved |
| 2026-07-18 | CI-only failure: log-hygiene-canary-e2e community-node boot exited status 1 on GitHub runners; harness diagnostics (rc2) revealed the real cause: Node 20 has no global WebSocket, so WebSocketRelayBackend throws at bin import when notify/announce relay URLs are set. Production images pin Node 22 | CI test job, meerkat-community-node.mjs via packages/sync websocket-relay-backend.ts | CI bumped to Node 22 (matches production Dockerfiles) in rc3; harness now surfaces status/signal/stderr on premature exit. Known limitation: self-hosting the bin on Node <22 with relay URLs set fails fast at boot (clear error, not silent) | Resolved |
| 2026-07-18 | Release-verify full matrix at ab29dabe: postgres upgrade-contract test asserted full schema inventory at version 2, but rooms schema only arrives at migration 17 (plan 42/44 integration drift, live-postgres suites never rerun post-merge) | packages/meerkat-relay/src/postgres/__tests__/postgres-integration.test.ts | Assertion narrowed to v2-era schemas (rooms excluded + asserted absent); full-inventory assertion added after migrate-to-latest; 171/171 live-postgres tests green locally | Resolved |
| 2026-07-18 | Quota serialization test born broken in 234d7492: first test in file books fixture 10 bytes against shared OWNER_HASH, exhausting the quota test 10-byte cap (both racers quota_exceeded) | packages/meerkat-relay/src/postgres/__tests__/archive-lifecycle-store.integration.test.ts | Test now uses a dedicated per-test owner hash; store logic was correct | Resolved |
| 2026-07-18 | CI Node 20 vs production images Node 22: AWS SDK NodeVersionSupport warning on stderr broke the s3-hosted log-hygiene assertion (empty-stderr contract) on runners only | .github/workflows/ci.yml, hosted-object-store-ingest.integration.test.ts | Both workflows bumped to node-version 22, matching packages/meerkat-relay Dockerfiles | Resolved |
| 2026-07-18 | relay-image healthz gate raced a cold tsx boot: curl --retry does not retry empty reply (exit 52) from docker-proxy before the service listens | .github/workflows/ci.yml relay-image job | Replaced with bounded poll loop + docker logs on failure, in ci.yml and release-verify.yml | Resolved |
| 2026-07-18 | Perf slope gate (createHostedAuthBearer) flaked under V8 coverage instrumentation on shared runners; same commit passed uninstrumented | packages/sync hosted-auth.function-gate.test.ts via test:coverage:ci | MYLIFE_PERF_GATES=off guard added to all 9 function-quality helper copies, set only in coverage CI jobs; uninstrumented test job still enforces budgets | Resolved |
| 2026-07-18 | rc3 release-verify: meerkat-postgres step failed with 171/171 tests passing; 2 uncaught pg idle-client errors from DROP DATABASE WITH (FORCE) teardown racing pool.end() on slow runners | packages/meerkat-relay postgres integration suites | No-op idle-client error handlers added to all 84 Pool constructions across 35 test files (pg mandates a pool error listener); suite green locally | Resolved |
| 2026-07-18 | rc3 release-verify: relay-image smoke failed; hosted-service bin fail-closed fatal state_authority_unavailable because the CI smoke env predates plan 44 MEERKAT_DEPLOYMENT_PROFILE enforcement; --rm hid the logs | .github workflows relay-image job, Dockerfile.platform | Reproduced locally in docker; smoke env now sets MEERKAT_DEPLOYMENT_PROFILE=self-host + MEERKAT_STORE_BACKEND=file; image boots, healthz ok. Fail-closed behavior was correct | Resolved |
| 2026-07-18 | First-ever CI run of web playwright e2e: 2 failed / 3 flaky. Root causes: next dev on-demand compile stalls + Next dev-tools issue badge overlaying the sidebar Settings link after benign ECONNRESET aborts (click interception), in-tab reminders tray overlaying card action buttons, and stale specs (MyRecipes renamed BestChef) | apps/web/e2e, playwright.config.ts | CI now runs next build + next start (no dev overlay, no on-demand compile); notifications permission granted in context so the tray never renders; spec renamed to BestChef. Product pages verified working via failure screenshots | Resolved |
| 2026-07-18 | Headless Chromium reports legacy Notification.permission static as denied even when context permission is granted (permissions.query returns granted); RemindersProvider reads the static, so its prompt overlays bottom-right buttons in every headless run and intercepts clicks | apps/web/components/RemindersProvider.tsx, e2e | e2e helper now injects an init script forcing the static to granted (matches real-browser granted state). Post-launch product consideration: dual-check permissions.query in the provider | Resolved |
| 2026-07-18 | Web e2e api-auth spec relied on the dev-only legacy tokenless userId fallback; production strict mode (MYLIFE_ACTOR_IDENTITY_STRICT_MODE default-on in production) correctly returns 401, so the spec failed only under the new prod-server CI e2e | apps/web/e2e/api-auth-and-pipeline.spec.ts | Spec now mints real actor identity tokens via issueActorIdentityToken with the e2e secret and sends x-actor-identity-token headers | Resolved |
| 2026-07-18 | rc4 release-verify: 1 of 2375 sync tests failed - createRevenueCatAppUserId perf slope gate (ratio 1.93 vs 1.80 on 2.5-4.8ms medians) in the UNINSTRUMENTED test job; shared-runner scheduler noise at millisecond scale, same class as the coverage flake | CI test job, packages/sync revenuecat-app-user-id.function-gate.test.ts | MYLIFE_PERF_GATES=off extended to CI test jobs in both workflows; perf gates remain enforced locally (pre-commit function gate + local suites) and release perf evidence comes from Step 15 load/soak budgets | Resolved |
| 2026-07-18 | MYLIFE_PERF_GATES: off in workflow YAML parsed bare off as boolean false, so the env reached the guard as the string false and === off never matched; perf gates were never disabled in CI (rc5 push CI failed another slope gate while release-verify passed on luck) | .github workflows, 9x function-quality.ts guard | YAML value quoted ("off") and guard hardened to accept off/false/0/no; verified via yaml parse + runs under all three env modes | Resolved |
| 2026-07-18 | Turbo 2.x strict env default stripped undeclared env vars from turbo-run tasks: MYLIFE_PERF_GATES never reached vitest through pnpm test (rc6 push CI slope flake at a SHA with the quoted YAML + hardened guard), and VITEST_SEED was silently absent from CI test runs (never actually seeded) | turbo.json test/test:coverage tasks | env: [MYLIFE_PERF_GATES, VITEST_SEED] declared on both tasks; verified via turbo --dry=json environmentVariables | Resolved |
| 2026-07-18 | rc7: coverage job failed on @mylife/health at 68.52% vs 70% threshold - first run where MYLIFE_PERF_GATES=off actually reached vitest (turbo env fix), revealing that src/test/function-quality.ts measurement loops were counted in product coverage and inflating it | modules/health vitest coverage | Perf-gate helpers now run one functional smoke pass when disabled (9 files); src/test/** excluded from coverage collection in the 4 threshold-bearing packages (health 70.06%, intelligence 98.45% verified locally; coverage now measures product code only) | Resolved |
| 2026-07-20 | FAIL  app/(root)/data/__tests__/age-gate.test.ts (minimum-age clamping) during age-gate development | apps/meerkat mobile age-gate adapter | Transient mid-development failure from the 2026-07-20 legal-fixes session; fixed before commit 04b38a3c. Re-run 2026-07-20 (plan 51 session): 6/6 green | Resolved |
| 2026-07-20 | Plan 51 P0 commit landed on main instead of the feature branch: a concurrent session (Step 3 legal docs, 15d2f464) checked out main under the same worktree between my branch creation and commit | git workflow, plan 51 session | Fast-forwarded feature/meerkat-plan51-verification-account to include both commits, restored main to 15d2f464 via git reset --keep. Rule reinforced: verify git branch --show-current immediately before every commit when sessions share a worktree | Resolved |
| 2026-07-20 | Auto-logged Vitest failures captured mid-development during parallel Plan 51 agent work (community-node-submit-credential, account-core store_adult, account-service-http finalize, web account deletion) | Plan 51 verification-account, four packages | In-flight churn while four subagents edited shared files against an evolving contract (epoch-key route, quota ordering, credentialHeaders convergence). All resolved before commit; final suites green: relay 1587, sync 2406, mobile 1408, web 1003, check:parity exit 0 | Resolved |
| 2026-07-21 | release-verify dispatched with a hand-typed SHA whose tail did not exist (719d52f6a2c1... vs real 719d52f6a7de...); run failed at checkout | gh workflow run release-verify.yml | Re-dispatched with $(git rev-parse); rule: never hand-type SHAs into dispatch inputs | Resolved |
| 2026-07-21 | rc10 full matrix: plan 51 shipped 2 failing live-postgres tests (v2 schema assertion rotted again when migration 18 added account/credential schemas; account cascade test re-inserted an issuance row for a deleted account, correctly rejected by the FK) - env-gated suite not run before merge | packages/meerkat-relay postgres integration tests | v2 assertion frozen to an explicit v2-era schema list (stops rotting); cascade proof rewritten as row-absence check. 35 files / 176 tests green locally | Resolved |
| 2026-07-21 | rc10 audit red: 8 new upstream advisories since last green scan (tar CVSS 9.2, shell-quote 8.7, brace-expansion 7.7, fast-uri 7.5, js-yaml 7.5) | pnpm-lock.yaml via osv-scanner | Override targets bumped to patched releases (tar 7.5.19, shell-quote 1.9.0, js-yaml 4.3.0, fast-uri 3.1.3, brace-expansion 1.1.16/5.0.7); local osv-scanner: No issues found | Resolved |
| 2026-07-21 | rc10/rc11 e2e: meerkat-web launch-path specs timed out behind plan 51 AgeGateOverlay (mounts before onboarding on every fresh profile; specs never answered it). Web e2e itself 11/11 green | apps/meerkat-web/e2e/launch-paths.spec.ts | passAgeGate helper fills an adult birth date after every fresh navigation; 7/7 green vs live relay | Resolved |
| 2026-07-24 | Community creation from ANY template fails on web with "cannot start a transaction within a transaction"; nothing is created. Web-only parity drift: `commitCommunityTemplate` calls `storeOwnedCommunity` INSIDE `db.transaction`, and that path mints the first epoch via `createGroupCommit`, which opens its own transaction. The browser sql.js adapter issues a raw BEGIN with no savepoint/depth tracking. Mobile's twin documents this exact hazard and orders the calls correctly | apps/meerkat-web/src/lib/community-template-commit.ts; meerkat-data.ts:1818 (storeOwnedCommunity); packages/sync/src/protocol/group-keys.ts:199; storage/browser-database-adapter.ts:128 | Fixed 2026-07-29 on fix/meerkat-rc13-defects: web now mirrors the mobile ordering (storeOwnedCommunity outside the transaction, libraries+identity in one transaction, purgeLocalCommunity on failure), commit c3336a7a; the CI blind spot closed by making createInMemoryTestDatabase refuse nested transactions like the shipped adapters, commit f09bf5bc (pre-fix code fails 5 tests under it). Launch-path e2e now creates chat-first and library-first templates on the real browser adapter. Live testbed: pre-fix repro then all six templates created (artifacts/meerkat-testbed/shots/rc13-*) | Resolved |
| 2026-07-24 | Sync fails with "No relay URL configured." while the panel above it still reads "Free server reachable". Free-default relay is health-gated on a `/healthz` probe cached for RELAY_PROBE_TTL_MS=60s, but the probe is only ever written on mount (provider boot + ConnectionStatusCard useEffect, no interval). Any sync attempted >60s after the last mount loses its relay URL. Pairing two devices always takes longer than 60s, so the first sync a new pair attempts lands in the window | apps/meerkat-web/src/lib/MeerkatProvider.tsx:1204; ui/sync/ConnectionStatusCard.tsx:118; meerkat-data.ts:160; apps/meerkat/app/(root)/data/db.ts:183; packages/sync/src/transport/default-relay.ts:100 | Fixed 2026-07-29 on fix/meerkat-rc13-defects, commit 2119075e: ensureEffectiveRelayUrl(db) re-probes a stale/missing default probe on demand in EVERY dial path (web + mobile), and both ConnectionStatusCards re-probe on the cache TTL while mounted so the pill and the dial read the same mk_relay_probe row. User-URL bypass, opt-out, and fresh-failure semantics unchanged (twin unit suites pin them). Live testbed: pre-fix repro ("No relay URL configured" after 70s), post-fix "Session completed · sent 2 / received 0" on the free default after the same wait (shots/rc13-*) | Resolved |
