# 2026-07-04 Main Merge Landing

## Goal

Founder request: review git history across MyLife (Meerkat, BestChef, MyNews, DoWork and everything else in flight) and get all the code safely and completely merged into main. Founder approved via check-in: merge all 3 live branches, fast-forward + merge-commit strategy, push main + feature branches, full cleanup.

## Starting state

- `main` == `origin/main` at `4ae6d19e`.
- `feature/meerkat-launch-finish` (primary checkout): 119 commits ahead, main a clean ancestor. Meerkat plans 19/20/21/23/27/28/30/31/32 (DMs on both surfaces, member removal, transport policies, UX parity) + BestChef plan 33 Phases 0-2.1 + moderator console. Uncommitted: staged BestChef Phase 2.2 + cross-session docs/ledgers.
- `feature/dowork-trainer-launch` (worktree): 14 commits, plan 36 trainer platform waves 1-3.
- `feature/mynews-p0-scaffold` (worktree): 29 commits, MyNews P0+P1, plus uncommitted Phase 2 auth WIP.
- `feature/mylife-improvements-sprints`: all 42 commits verified patch-equivalent to main (`git cherry`).
- 13 other local branches fully merged; 15 stashes all superseded (big 06-10 one byte-identical in HEAD); 4 stale remote branches (Feb-Mar); 2 stale worktrees.

## What happened

1. **Concurrent-session discovery.** While preparing commits, another live session committed BestChef Phase 2.2 (`a8594bcb`) + docs (`bcce4bae`) in the primary checkout, then started Phase 2.3 files. Decision: never switch the shared primary checkout off the branch; all main-side work ran in a new worktree `.claude/worktrees/main-merge`. Earlier "flaky" suite failures (meerkat-web, bestchef gate crash) were CPU contention with that session; both suites verified green in isolation.
2. **Pre-merge commits on the branch** (primary checkout, no branch switch):
   - `6b28884c` test(web): entitlement-issuer expectation gains `meerkat:hosted-storage` (the known pre-existing failure; Plan 22 Stage-0 added the feature to billing-config defaults).
   - `375db0a5` chore: session ledgers, 15 report/session docs, meerkat-host.mjs exec bit, firecrawl MCP removal.
3. **Merges in the main-merge worktree:**
   - Fast-forward main -> `375db0a5` (121 commits).
   - `78138e21` merge dowork (conflicts: memory.md + errors_log.md session tables; resolved by union).
   - `6309c915` merge mynews (conflicts: package.json parity chain unioned to include BOTH `check:mynews-parity` and `check:i18n-parity`; pnpm-lock regenerated by `pnpm install`).
4. **Full verification caught 2 real cross-branch integration bugs** (each branch green in isolation, red together):
   - sync `module-reconciliation.test.ts` hardcoded 40 ModuleIds; MyNews made it 41. Fixed both assertions.
   - `apps/mynews/app.json` never committed: root `.gitignore` has a global `app.json` pattern (bestchef/dowork were force-added historically). Copied from the MyNews worktree and `git add -f`.
   - Fix commit: `edd6b2e3`.
5. **Final verify on merged main: GREEN.** typecheck 125/125, `pnpm test` 129/129 turbo tasks, full parity chain (incl. mynews + i18n legs, 21 catalogs at 100%), generated-artifacts guard.
6. **Pushed:** main `4ae6d19e -> edd6b2e3`, `feature/meerkat-launch-finish` -> `375db0a5`, dowork + mynews as new remote branches.
7. **Cleanup:** deleted 15 merged local branches (incl. bestchef-launch-hardening after verifying the 7 Yearn commits ARE on main - the memory.md "unpushed" claim was stale; manhattan-scaffold; plan19-p9-archive; salvage + old meerkat branches); removed wizardly-roentgen (discarded a local settings.json hook tweak) + main-relay-rendezvous-fix worktrees; `git stash clear` (15 superseded); deleted 4 stale remote branches (module-expansion-batch-all, mysurf-full-parity, add-gstack-claude-md, budget-ceo-review).

## Remaining / handed forward

- `git branch -D feature/mylife-improvements-sprints`: policy hook blocks force-delete; founder should run it (content verified patch-equivalent in main).
- DoWork + MyNews worktrees gained NEWER commits during the merge (active sessions) and MyNews has uncommitted auth WIP; those deltas plus any BestChef Phase 2.3+ commits on launch-finish need a future re-merge (the main-merge worktree is kept for that).
- MyNews Task 14 (`post_type` widening) is now UNBLOCKED (was gated on the Meerkat merge).
- memory.md is far over its 80-line budget; archiving old Sessions rows deferred to avoid conflicting with the 3 concurrently active sessions.

## Verification evidence

- Branch pre-merge: typecheck 125/125; apps/bestchef 303/303; staged gate green; apps/web 376 passed/4 skipped; meerkat-web 468/468.
- Merged main: TEST_EXIT=0 (129/129 tasks), PARITY_EXIT=0, ARTIFACT_EXIT=0, TYPECHECK_EXIT=0.
