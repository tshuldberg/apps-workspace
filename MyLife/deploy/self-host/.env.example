# MyLife Self-Host Environment Variables
# Copy this file to .env and fill in all required values before running docker-compose up.

# --- PostgreSQL ---
POSTGRES_DB=mylife
POSTGRES_USER=mylife
# Required. Database password for the mylife user.
POSTGRES_PASSWORD=

# Constructed from the above. Override only if using an external database.
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# --- MinIO (S3-compatible object storage) ---
# Required. Credentials for the MinIO root account.
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
# Bucket name for stored objects (default: mylife-objects).
# MINIO_BUCKET=mylife-objects
# Port for the MinIO S3 API (default: 9000).
# MINIO_API_PORT=9000
# Port for the MinIO web console, bound to localhost only (default: 9001).
# MINIO_CONSOLE_PORT=9001

# --- API Server ---
# Required. The origin allowed for CORS requests (e.g., https://mylife.example.com).
CORS_ORIGIN=
# Port the API listens on (default: 8787).
# MYLIFE_API_PORT=8787

# --- Admin Account ---
# Required. Email and password for the initial self-host admin user.
SELF_HOST_ADMIN_EMAIL=
SELF_HOST_ADMIN_PASSWORD=

# --- Entitlement Keys (optional) ---
# Keys for entitlement sync and issuance. Leave empty to disable.
# MYLIFE_ENTITLEMENT_SYNC_KEY=
# MYLIFE_ENTITLEMENT_ISSUER_KEY=

# --- Storage Health Check (optional) ---
# URL used by the API to verify MinIO is reachable (default: http://minio:9000/minio/health/live).
# STORAGE_HEALTHCHECK_URL=http://minio:9000/minio/health/live
