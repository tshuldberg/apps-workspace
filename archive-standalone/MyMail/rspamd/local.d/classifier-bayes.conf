# Bayesian classifier - learns from user spam/ham actions
backend = "redis";
servers = "redis:6379";
autolearn = true;

# Autolearn thresholds
autolearn {
    spam_threshold = 6.0;
    ham_threshold = -0.5;
}

# Per-user Bayes when available
per_user = false;
