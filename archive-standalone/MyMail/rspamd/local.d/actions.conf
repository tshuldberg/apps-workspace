# Spam score thresholds
# reject: message is rejected outright
# add_header: message is delivered with spam headers
# greylist: message is temporarily deferred
# no_action: below this score, no spam action taken

reject = 15;
add_header = 6;
greylist = 4;
