# System monitor

- System commands use execFile with arguments, never shell-based execution.
- Preserve daemon threshold/state behavior and launchd lifecycle. Validate the changed monitor/daemon path; use package.json and README.md for commands and setup.
